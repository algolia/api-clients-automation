{{^autoCreateClient}}const client = {{/autoCreateClient}}{{^isCompositionClient}}{{{clientName}}}{{/isCompositionClient}}{{#isCompositionClient}}compositionClient{{/isCompositionClient}}(
  '{{parametersWithDataTypeMap.appId.value}}',
  '{{parametersWithDataTypeMap.apiKey.value}}'
  {{#isStandaloneClient}},
  {
    {{#useEchoRequester}}
    requester: nodeEchoRequester(),
    {{/useEchoRequester}}
    {{#hasCustomHosts}}
    hosts:[
    {{#customHosts}}
    {
      url: 'localhost',
      port: {{port}},
      accept: 'readWrite',
      protocol: 'http'
    },
    {{/customHosts}}
    ]
    {{/hasCustomHosts}} 
  }
  {{/isStandaloneClient}}
){{^isStandaloneClient}}.{{{initMethod}}}(
  {
    options: {
    {{#useEchoRequester}}
      requester: nodeEchoRequester(),
    {{/useEchoRequester}}
    {{#hasCustomHosts}}
      hosts:[
      {{#customHosts}}
      {
        url: 'localhost',
        port: {{port}},
        accept: 'readWrite',
        protocol: 'http'
      },
      {{/customHosts}}
      ]
      {{/hasCustomHosts}}
  },
 {{#hasRegionalHost}}
 // @ts-ignore
 region: '{{{parametersWithDataTypeMap.region.value}}}'
 {{/hasRegionalHost}}
})
{{/isStandaloneClient}};
{{> snippets/import}}
import com.algolia.client.model.search.*

suspend fun useConditionlessRule() {
  {{> snippets/init}}

  val objectID = "a-rule-id";

  val rule = Rule(
      objectID = objectID,
      consequence = Consequence(
          // Set relevant consequence
      ),
      // Set validity (optional)
      validity = listOf(
          TimeRange(
              from = 1_688_774_400,
              until = 1_738_972_800
          )
      )
  )

  client.{{#dynamicSnippet}}saveRule{{/dynamicSnippet}}
}
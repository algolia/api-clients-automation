
  /**
   * Sets [{{{name}}}] from [block]. Last write wins: replaces any earlier value. An empty block sends
   * `[]`, like the settings helpers (`searchableAttributes { }` etc.) and version 2.
{{#kdocExtra}}
   *
   * {{{kdocExtra}}}
{{/kdocExtra}}
   */
  public fun {{{name}}}(block: {{{receiver}}}.() -> Unit) {
    this.{{{name}}} = {{{receiver}}}().apply(block).build()
  }

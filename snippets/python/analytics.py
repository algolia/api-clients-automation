# Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
# >IMPORT
from algoliasearch.analytics.client import AnalyticsClientSync
# IMPORT<


def snippet_for_custom_delete():
    """
    Snippet for the customDelete method.

    allow del method for a custom path with minimal parameters
    """
    # >SEPARATOR customDelete default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.custom_delete(
        path="test/minimal",
    )

    print(response)

    # Skip deserialization
    raw_response = client.custom_delete_with_http_info(
        path="test/minimal",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_custom_get():
    """
    Snippet for the customGet method.

    allow get method for a custom path with minimal parameters
    """
    # >SEPARATOR customGet default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.custom_get(
        path="test/minimal",
    )

    print(response)

    # Skip deserialization
    raw_response = client.custom_get_with_http_info(
        path="test/minimal",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_custom_post():
    """
    Snippet for the customPost method.

    allow post method for a custom path with minimal parameters
    """
    # >SEPARATOR customPost default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.custom_post(
        path="test/minimal",
    )

    print(response)

    # Skip deserialization
    raw_response = client.custom_post_with_http_info(
        path="test/minimal",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_custom_put():
    """
    Snippet for the customPut method.

    allow put method for a custom path with minimal parameters
    """
    # >SEPARATOR customPut default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.custom_put(
        path="test/minimal",
    )

    print(response)

    # Skip deserialization
    raw_response = client.custom_put_with_http_info(
        path="test/minimal",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_add_to_cart_rate():
    """
    Snippet for the getAddToCartRate method.

    get getAddToCartRate with minimal parameters
    """
    # >SEPARATOR getAddToCartRate default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_add_to_cart_rate(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_add_to_cart_rate_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_average_click_position():
    """
    Snippet for the getAverageClickPosition method.

    get getAverageClickPosition with minimal parameters
    """
    # >SEPARATOR getAverageClickPosition default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_average_click_position(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_average_click_position_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_click_positions():
    """
    Snippet for the getClickPositions method.

    get getClickPositions with minimal parameters
    """
    # >SEPARATOR getClickPositions default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_click_positions(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_click_positions_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_click_through_rate():
    """
    Snippet for the getClickThroughRate method.

    get getClickThroughRate with minimal parameters
    """
    # >SEPARATOR getClickThroughRate default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_click_through_rate(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_click_through_rate_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_conversion_rate():
    """
    Snippet for the getConversionRate method.

    get getConversationRate with minimal parameters
    """
    # >SEPARATOR getConversionRate default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_conversion_rate(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_conversion_rate_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_no_click_rate():
    """
    Snippet for the getNoClickRate method.

    get getNoClickRate with minimal parameters
    """
    # >SEPARATOR getNoClickRate default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_no_click_rate(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_no_click_rate_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_no_results_rate():
    """
    Snippet for the getNoResultsRate method.

    get getNoResultsRate with minimal parameters
    """
    # >SEPARATOR getNoResultsRate default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_no_results_rate(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_no_results_rate_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_purchase_rate():
    """
    Snippet for the getPurchaseRate method.

    get getPurchaseRate with minimal parameters
    """
    # >SEPARATOR getPurchaseRate default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_purchase_rate(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_purchase_rate_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_revenue():
    """
    Snippet for the getRevenue method.

    get getRevenue with minimal parameters
    """
    # >SEPARATOR getRevenue default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_revenue(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_revenue_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_searches_count():
    """
    Snippet for the getSearchesCount method.

    get getSearchesCount with minimal parameters
    """
    # >SEPARATOR getSearchesCount default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_searches_count(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_searches_count_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_searches_no_clicks():
    """
    Snippet for the getSearchesNoClicks method.

    get getSearchesNoClicks with minimal parameters
    """
    # >SEPARATOR getSearchesNoClicks default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_searches_no_clicks(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_searches_no_clicks_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_searches_no_results():
    """
    Snippet for the getSearchesNoResults method.

    get getSearchesNoResults with minimal parameters
    """
    # >SEPARATOR getSearchesNoResults default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_searches_no_results(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_searches_no_results_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_status():
    """
    Snippet for the getStatus method.

    get getStatus with minimal parameters
    """
    # >SEPARATOR getStatus default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_status(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_status_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_top_countries():
    """
    Snippet for the getTopCountries method.

    get getTopCountries with minimal parameters
    """
    # >SEPARATOR getTopCountries default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_top_countries(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_top_countries_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_top_filter_attributes():
    """
    Snippet for the getTopFilterAttributes method.

    get getTopFilterAttributes with minimal parameters
    """
    # >SEPARATOR getTopFilterAttributes default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_top_filter_attributes(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_top_filter_attributes_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_top_filter_for_attribute():
    """
    Snippet for the getTopFilterForAttribute method.

    get getTopFilterForAttribute with minimal parameters
    """
    # >SEPARATOR getTopFilterForAttribute default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_top_filter_for_attribute(
        attribute="myAttribute",
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_top_filter_for_attribute_with_http_info(
        attribute="myAttribute",
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_top_filters_no_results():
    """
    Snippet for the getTopFiltersNoResults method.

    get getTopFiltersNoResults with minimal parameters
    """
    # >SEPARATOR getTopFiltersNoResults default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_top_filters_no_results(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_top_filters_no_results_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_top_hits():
    """
    Snippet for the getTopHits method.

    get getTopHits with minimal parameters
    """
    # >SEPARATOR getTopHits default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_top_hits(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_top_hits_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_top_searches():
    """
    Snippet for the getTopSearches method.

    get getTopSearches with minimal parameters
    """
    # >SEPARATOR getTopSearches default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_top_searches(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_top_searches_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_get_users_count():
    """
    Snippet for the getUsersCount method.

    get getUsersCount with minimal parameters
    """
    # >SEPARATOR getUsersCount default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    response = client.get_users_count(
        index="index",
    )

    print(response)

    # Skip deserialization
    raw_response = client.get_users_count_with_http_info(
        index="index",
    )

    print(raw_response.raw_data)

    # >LOG
    # use the class directly
    print(response)

    # print the JSON response
    print(response.to_json())
    # SEPARATOR<


def snippet_for_set_client_api_key():
    """
    Snippet for the setClientApiKey method.

    switch API key
    """
    # >SEPARATOR setClientApiKey default
    # Initialize the client
    # In an asynchronous context, you can use AnalyticsClient instead, which exposes the exact same methods.
    client = AnalyticsClientSync(
        "ALGOLIA_APPLICATION_ID", "ALGOLIA_API_KEY", "ALGOLIA_APPLICATION_REGION"
    )

    # Call the API
    client.set_client_api_key(
        api_key="updated-api-key",
    )

    # Skip deserialization
    client.set_client_api_key_with_http_info(
        api_key="updated-api-key",
    )

    # >LOG
    # SEPARATOR<

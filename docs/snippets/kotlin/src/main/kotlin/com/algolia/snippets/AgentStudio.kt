// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost
// - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
package com.algolia.snippets

// >IMPORT
// IMPORT<
import com.algolia.client.api.AgentStudioClient
import com.algolia.client.configuration.*
import com.algolia.client.model.agentstudio.*
import com.algolia.client.transport.*
import kotlin.system.exitProcess
import kotlinx.serialization.json.*

class SnippetAgentStudioClient {
  suspend fun snippetForCreateAgent() {
    // >SEPARATOR createAgent createAgent with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createAgent(
        agentConfigCreate =
          AgentConfigCreate(name = "test-agent", instructions = "You are a helpful assistant.")
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCreateAgent1() {
    // >SEPARATOR createAgent createAgent with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createAgent(
        agentConfigCreate =
          AgentConfigCreate(
            name = "test-agent",
            description = "A test agent for CTS",
            providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a",
            model = "gpt-4",
            instructions = "You are a helpful assistant.",
            config =
              buildJsonObject {
                put("sendUsage", JsonPrimitive(true))
                put("sendReasoning", JsonPrimitive(true))
                put("temperature", JsonPrimitive(0.7))
                put("max_tokens", JsonPrimitive(1500))
              },
            tools = listOf(StartPart(type = "start")),
          )
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCreateAgentCompletion() {
    // >SEPARATOR createAgentCompletion createAgentCompletion with v4 messages
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createAgentCompletion(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        compatibilityMode = CompatibilityMode.entries.first { it.value == "ai-sdk-4" },
        agentCompletionRequest =
          AgentCompletionRequest(
            messages =
              MessagesUnion.ofListOfMessageV4(
                listOf(UserMessageV4(role = "user", content = "Hello, how are you?"))
              )
          ),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCreateAgentCompletion1() {
    // >SEPARATOR createAgentCompletion createAgentCompletion with v5 messages and all query params
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createAgentCompletion(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        compatibilityMode = CompatibilityMode.entries.first { it.value == "ai-sdk-5" },
        agentCompletionRequest =
          AgentCompletionRequest(
            messages =
              MessagesUnion.ofListOfMessageV5(
                listOf(
                  UserMessageV5(
                    role = "user",
                    parts = listOf(TextPartV5(type = "text", text = "What is Algolia?")),
                  )
                )
              ),
            id = "test-conversation-id",
          ),
        stream = false,
        cache = false,
        memory = false,
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCreateAgentCompletion2() {
    // >SEPARATOR createAgentCompletion createAgentCompletion with test configuration
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createAgentCompletion(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        compatibilityMode = CompatibilityMode.entries.first { it.value == "ai-sdk-4" },
        agentCompletionRequest =
          AgentCompletionRequest(
            messages =
              MessagesUnion.ofListOfMessageV4(
                listOf(UserMessageV4(role = "user", content = "Hello"))
              ),
            configuration =
              AgentTestConfiguration(
                instructions = "Test instructions override",
                config = buildJsonObject { put("temperature", JsonPrimitive(0.2)) },
                tools = listOf(StartPart(type = "start")),
              ),
          ),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCreateProvider() {
    // >SEPARATOR createProvider createProvider with OpenAI
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createProvider(
        providerAuthenticationCreate =
          ProviderAuthenticationCreate(
            name = "My OpenAI Provider",
            providerName = ProviderName.entries.first { it.value == "openai" },
            input = OpenAIProviderInput(apiKey = "sk-test-key-1234"),
          )
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCreateProvider1() {
    // >SEPARATOR createProvider createProvider with Azure OpenAI
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.createProvider(
        providerAuthenticationCreate =
          ProviderAuthenticationCreate(
            name = "My Azure Provider",
            providerName = ProviderName.entries.first { it.value == "azure_openai" },
            input =
              AzureOpenAIProviderInput(
                apiKey = "az-test-key-5678",
                azureEndpoint = "https://my-resource.openai.azure.com",
                azureDeployment = "gpt-4o",
              ),
          )
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomDelete() {
    // >SEPARATOR customDelete allow del method for a custom path with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.customDelete(path = "test/minimal")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomDelete1() {
    // >SEPARATOR customDelete allow del method for a custom path with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customDelete(path = "test/all", parameters = mapOf("query" to "parameters"))

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomGet() {
    // >SEPARATOR customGet allow get method for a custom path with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.customGet(path = "test/minimal")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomGet1() {
    // >SEPARATOR customGet allow get method for a custom path with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customGet(path = "test/all", parameters = mapOf("query" to "parameters with space"))

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomGet2() {
    // >SEPARATOR customGet requestOptions should be escaped too
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customGet(
        path = "test/all",
        parameters = mapOf("query" to "to be overridden"),
        requestOptions =
          RequestOptions(
            urlParameters =
              buildMap {
                put("query", "parameters with space")
                put("and an array", listOf("array", "with spaces"))
              },
            headers = buildMap { put("x-header-1", "spaces are left alone") },
          ),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost() {
    // >SEPARATOR customPost allow post method for a custom path with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.customPost(path = "test/minimal")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost1() {
    // >SEPARATOR customPost allow post method for a custom path with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/all",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("body", JsonPrimitive("parameters")) },
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost2() {
    // >SEPARATOR customPost requestOptions can override default query parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions =
          RequestOptions(urlParameters = buildMap { put("query", "myQueryParameter") }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost3() {
    // >SEPARATOR customPost requestOptions merges query parameters with default ones
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions =
          RequestOptions(urlParameters = buildMap { put("query2", "myQueryParameter") }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost4() {
    // >SEPARATOR customPost requestOptions can override default headers
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions =
          RequestOptions(headers = buildMap { put("x-algolia-api-key", "ALGOLIA_API_KEY") }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost5() {
    // >SEPARATOR customPost requestOptions merges headers with default ones
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions =
          RequestOptions(headers = buildMap { put("x-algolia-api-key", "ALGOLIA_API_KEY") }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost6() {
    // >SEPARATOR customPost requestOptions queryParameters accepts booleans
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions = RequestOptions(urlParameters = buildMap { put("isItWorking", true) }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost7() {
    // >SEPARATOR customPost requestOptions queryParameters accepts integers
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions = RequestOptions(urlParameters = buildMap { put("myParam", 2) }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost8() {
    // >SEPARATOR customPost requestOptions queryParameters accepts list of string
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions =
          RequestOptions(urlParameters = buildMap { put("myParam", listOf("b and c", "d")) }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost9() {
    // >SEPARATOR customPost requestOptions queryParameters accepts list of booleans
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions =
          RequestOptions(urlParameters = buildMap { put("myParam", listOf(true, true, false)) }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPost10() {
    // >SEPARATOR customPost requestOptions queryParameters accepts list of integers
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPost(
        path = "test/requestOptions",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("facet", JsonPrimitive("filters")) },
        requestOptions = RequestOptions(urlParameters = buildMap { put("myParam", listOf(1, 2)) }),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPut() {
    // >SEPARATOR customPut allow put method for a custom path with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.customPut(path = "test/minimal")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForCustomPut1() {
    // >SEPARATOR customPut allow put method for a custom path with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.customPut(
        path = "test/all",
        parameters = mapOf("query" to "parameters"),
        body = buildJsonObject { put("body", JsonPrimitive("parameters")) },
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForDeleteAgent() {
    // >SEPARATOR deleteAgent default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.deleteAgent(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForDeleteAgentConversations() {
    // >SEPARATOR deleteAgentConversations deleteAgentConversations without filters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.deleteAgentConversations(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForDeleteAgentConversations1() {
    // >SEPARATOR deleteAgentConversations deleteAgentConversations with date filters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.deleteAgentConversations(
      agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
      startDate = "2024-01-01",
      endDate = "2024-06-30",
    )

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForDeleteConversation() {
    // >SEPARATOR deleteConversation default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.deleteConversation(
      agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
      conversationId = "test-conversation-id",
    )

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForDeleteProvider() {
    // >SEPARATOR deleteProvider default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.deleteProvider(providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a")

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForDeleteUserData() {
    // >SEPARATOR deleteUserData default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.deleteUserData(userToken = "test-user-token")

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForExportConversations() {
    // >SEPARATOR exportConversations exportConversations without filters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.exportConversations(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForExportConversations1() {
    // >SEPARATOR exportConversations exportConversations with date filters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.exportConversations(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        startDate = "2024-01-01",
        endDate = "2024-12-31",
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetAgent() {
    // >SEPARATOR getAgent getAgent
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getAgent(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetAgent1() {
    // >SEPARATOR getAgent e2e get agent
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getAgent(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetApplicationConfiguration() {
    // >SEPARATOR getApplicationConfiguration getApplicationConfiguration
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getApplicationConfiguration()

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetApplicationConfiguration1() {
    // >SEPARATOR getApplicationConfiguration e2e getApplicationConfiguration
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getApplicationConfiguration()

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetConversation() {
    // >SEPARATOR getConversation default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.getConversation(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        conversationId = "test-conversation-id",
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetProvider() {
    // >SEPARATOR getProvider getProvider
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getProvider(providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetProvider1() {
    // >SEPARATOR getProvider e2e get provider
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getProvider(providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForGetUserData() {
    // >SEPARATOR getUserData default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.getUserData(userToken = "test-user-token")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForInvalidateAgentCache() {
    // >SEPARATOR invalidateAgentCache invalidateAgentCache without before
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.invalidateAgentCache(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForInvalidateAgentCache1() {
    // >SEPARATOR invalidateAgentCache invalidateAgentCache with before date
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.invalidateAgentCache(
      agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
      before = "2024-12-01",
    )

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListAgentConversations() {
    // >SEPARATOR listAgentConversations listAgentConversations with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listAgentConversations(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListAgentConversations1() {
    // >SEPARATOR listAgentConversations listAgentConversations with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.listAgentConversations(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        startDate = "2024-01-01",
        endDate = "2024-12-31",
        page = 2,
        limit = 10,
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListAgents() {
    // >SEPARATOR listAgents list agents with no params
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listAgents()

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListAgents1() {
    // >SEPARATOR listAgents list agents with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.listAgents(page = 2, limit = 5, providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListAgents2() {
    // >SEPARATOR listAgents e2e list agents
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listAgents(page = 1, limit = 2)

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListModels() {
    // >SEPARATOR listModels listModels
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listModels()

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListModels1() {
    // >SEPARATOR listModels e2e list models
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listModels()

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListProviderModels() {
    // >SEPARATOR listProviderModels default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listProviderModels(providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListProviders() {
    // >SEPARATOR listProviders listProviders with no params
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listProviders()

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListProviders1() {
    // >SEPARATOR listProviders listProviders with pagination
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listProviders(page = 2, limit = 5)

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForListProviders2() {
    // >SEPARATOR listProviders e2e list providers
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.listProviders(page = 1, limit = 2)

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForPublishAgent() {
    // >SEPARATOR publishAgent default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.publishAgent(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForSetClientApiKey() {
    // >SEPARATOR setClientApiKey default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    client.setClientApiKey(apiKey = "updated-api-key")

    // >LOG
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForUnpublishAgent() {
    // >SEPARATOR unpublishAgent default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response = client.unpublishAgent(agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff")

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForUpdateAgent() {
    // >SEPARATOR updateAgent updateAgent with minimal parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.updateAgent(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        agentConfigUpdate = AgentConfigUpdate(name = "updated-agent-name"),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForUpdateAgent1() {
    // >SEPARATOR updateAgent updateAgent with all parameters
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.updateAgent(
        agentId = "785b14e4-61a8-4c03-8028-bf2ba3ae85ff",
        agentConfigUpdate =
          AgentConfigUpdate(
            name = "updated-agent",
            description = "Updated description",
            providerId = "new-provider-id",
            model = "gpt-4o",
            instructions = "Updated instructions.",
            config = buildJsonObject { put("temperature", JsonPrimitive(0.5)) },
            tools = listOf(StartPart(type = "start")),
          ),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForUpdateConfiguration() {
    // >SEPARATOR updateConfiguration default
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.updateConfiguration(
        applicationConfigPatch = ApplicationConfigPatch(maxRetentionDays = 30)
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForUpdateProvider() {
    // >SEPARATOR updateProvider updateProvider
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.updateProvider(
        providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a",
        providerAuthenticationPatch = ProviderAuthenticationPatch(name = "Updated Provider Name"),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }

  suspend fun snippetForUpdateProvider1() {
    // >SEPARATOR updateProvider updateProvider with input
    // Initialize the client
    val client =
      AgentStudioClient(
        appId = "ALGOLIA_APPLICATION_ID",
        apiKey = "ALGOLIA_API_KEY",
        region = "ALGOLIA_APPLICATION_REGION",
      )

    // Call the API
    var response =
      client.updateProvider(
        providerId = "5e33d805-7892-4329-bda7-b524dc3fd04a",
        providerAuthenticationPatch =
          ProviderAuthenticationPatch(
            name = "Updated Provider",
            input = OpenAIProviderInput(apiKey = "sk-new-key-5678"),
          ),
      )

    // >LOG
    // print the response
    println(response)
    // SEPARATOR<

    exitProcess(0)
  }
}

// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.

// >IMPORT
import { advancedPersonalizationClient } from '@algolia/advanced-personalization';
// IMPORT<

// Snippet for the computeRealtimeUser method.
//
// computeRealtimeUser
export async function snippetForComputeRealtimeUser(): Promise<void> {
  // >SEPARATOR computeRealtimeUser default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.computeRealtimeUser({ userToken: 'foo' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customDelete method.
//
// allow del method for a custom path with minimal parameters
export async function snippetForCustomDelete(): Promise<void> {
  // >SEPARATOR customDelete allow del method for a custom path with minimal parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customDelete({ path: 'test/minimal' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customDelete method.
//
// allow del method for a custom path with all parameters
export async function snippetForCustomDelete1(): Promise<void> {
  // >SEPARATOR customDelete allow del method for a custom path with all parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customDelete({ path: 'test/all', parameters: { query: 'parameters' } });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customGet method.
//
// allow get method for a custom path with minimal parameters
export async function snippetForCustomGet(): Promise<void> {
  // >SEPARATOR customGet allow get method for a custom path with minimal parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customGet({ path: 'test/minimal' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customGet method.
//
// allow get method for a custom path with all parameters
export async function snippetForCustomGet1(): Promise<void> {
  // >SEPARATOR customGet allow get method for a custom path with all parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customGet({ path: 'test/all', parameters: { query: 'parameters with space' } });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customGet method.
//
// requestOptions should be escaped too
export async function snippetForCustomGet2(): Promise<void> {
  // >SEPARATOR customGet requestOptions should be escaped too
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customGet(
    { path: 'test/all', parameters: { query: 'to be overridden' } },
    {
      queryParameters: { query: 'parameters with space', 'and an array': ['array', 'with spaces'] },
      headers: { 'x-header-1': 'spaces are left alone' },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// allow post method for a custom path with minimal parameters
export async function snippetForCustomPost(): Promise<void> {
  // >SEPARATOR customPost allow post method for a custom path with minimal parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost({ path: 'test/minimal' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// allow post method for a custom path with all parameters
export async function snippetForCustomPost1(): Promise<void> {
  // >SEPARATOR customPost allow post method for a custom path with all parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost({
    path: 'test/all',
    parameters: { query: 'parameters' },
    body: { body: 'parameters' },
  });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions can override default query parameters
export async function snippetForCustomPost2(): Promise<void> {
  // >SEPARATOR customPost requestOptions can override default query parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { query: 'myQueryParameter' },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions merges query parameters with default ones
export async function snippetForCustomPost3(): Promise<void> {
  // >SEPARATOR customPost requestOptions merges query parameters with default ones
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { query2: 'myQueryParameter' },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions can override default headers
export async function snippetForCustomPost4(): Promise<void> {
  // >SEPARATOR customPost requestOptions can override default headers
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      headers: { 'x-algolia-api-key': 'ALGOLIA_API_KEY' },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions merges headers with default ones
export async function snippetForCustomPost5(): Promise<void> {
  // >SEPARATOR customPost requestOptions merges headers with default ones
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      headers: { 'x-algolia-api-key': 'ALGOLIA_API_KEY' },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions queryParameters accepts booleans
export async function snippetForCustomPost6(): Promise<void> {
  // >SEPARATOR customPost requestOptions queryParameters accepts booleans
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { isItWorking: true },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions queryParameters accepts integers
export async function snippetForCustomPost7(): Promise<void> {
  // >SEPARATOR customPost requestOptions queryParameters accepts integers
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { myParam: 2 },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions queryParameters accepts list of string
export async function snippetForCustomPost8(): Promise<void> {
  // >SEPARATOR customPost requestOptions queryParameters accepts list of string
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { myParam: ['b and c', 'd'] },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions queryParameters accepts list of booleans
export async function snippetForCustomPost9(): Promise<void> {
  // >SEPARATOR customPost requestOptions queryParameters accepts list of booleans
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { myParam: [true, true, false] },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPost method.
//
// requestOptions queryParameters accepts list of integers
export async function snippetForCustomPost10(): Promise<void> {
  // >SEPARATOR customPost requestOptions queryParameters accepts list of integers
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPost(
    { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
    {
      queryParameters: { myParam: [1, 2] },
    },
  );

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPut method.
//
// allow put method for a custom path with minimal parameters
export async function snippetForCustomPut(): Promise<void> {
  // >SEPARATOR customPut allow put method for a custom path with minimal parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPut({ path: 'test/minimal' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the customPut method.
//
// allow put method for a custom path with all parameters
export async function snippetForCustomPut1(): Promise<void> {
  // >SEPARATOR customPut allow put method for a custom path with all parameters
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.customPut({
    path: 'test/all',
    parameters: { query: 'parameters' },
    body: { body: 'parameters' },
  });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the deleteUser method.
//
// deleteUser
export async function snippetForDeleteUser(): Promise<void> {
  // >SEPARATOR deleteUser default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.deleteUser({ userID: 'user-1' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the getConfig method.
//
// getConfig
export async function snippetForGetConfig(): Promise<void> {
  // >SEPARATOR getConfig default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.getConfig();

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the getRealtimeUser method.
//
// getRealtimeUser
export async function snippetForGetRealtimeUser(): Promise<void> {
  // >SEPARATOR getRealtimeUser default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.getRealtimeUser({ userToken: 'foo' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the getUser method.
//
// getUser
export async function snippetForGetUser(): Promise<void> {
  // >SEPARATOR getUser default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.getUser({ userID: 'user-1' });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the getUsers method.
//
// getUsers
export async function snippetForGetUsers(): Promise<void> {
  // >SEPARATOR getUsers default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.getUsers({ indices: ['indexName'] });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the putConfig method.
//
// putConfig
export async function snippetForPutConfig(): Promise<void> {
  // >SEPARATOR putConfig default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  const response = await client.putConfig({
    indices: [],
    personalizationReRanking: 'maximum',
    profileType: 'basic',
    features: { realtime: { enabled: false } },
  });

  // >LOG
  // print the response
  console.log(response);
  // SEPARATOR<
}

// Snippet for the setClientApiKey method.
//
// switch API key
export function snippetForSetClientApiKey(): void {
  // >SEPARATOR setClientApiKey default
  // Initialize the client
  // Replace 'eu' with your Algolia Application Region
  const client = advancedPersonalizationClient('ALGOLIA_APPLICATION_ID', 'ALGOLIA_API_KEY', 'eu');

  // Call the API
  client.setClientApiKey({ apiKey: 'updated-api-key' });

  // >LOG
  // SEPARATOR<
}

/// Whether the platform mints the Request-ID as the `x-algolia-request-id`
/// query parameter instead of the header; native platforms use the header.
const bool platformRequestIdAsQueryParameter = false;

/**
 * Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be
 * lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
 */
package com.algolia.client.model.agentstudio

import kotlinx.serialization.*
import kotlinx.serialization.json.*

/**
 * Compaction-specific telemetry, returned only by ``/context/compact``. Kept separate from
 * :class:`ContextStats` so the deterministic ``/context/trim`` contract stays free of LLM-shaped
 * fields. ``compacted`` makes the short-conversation passthrough detectable: without it a caller
 * cannot tell a no-op from a summary that happened to be short.
 *
 * @param compacted Whether a summary was actually produced. False means the payload passed through
 *   untouched (nothing older than the kept tail, or no summarizable text).
 * @param chunksProcessed Number of transcript chunks summarized in the map phase. 0 on passthrough,
 *   1 when the transcript fit in a single summarizer call.
 * @param passes Number of summarizer rounds. 1 for a direct summary; 2+ when chunk summaries had to
 *   be merged (map-reduce).
 * @param summarizerPromptTokens Provider-reported input tokens summed across every summarizer call.
 *   Null when the provider reported no usage metadata.
 * @param summarizerCompletionTokens Provider-reported output tokens summed across every summarizer
 *   call. Null when the provider reported no usage metadata.
 * @param targetTokensEstimate The requested output budget, echoed back when `targetTokensEstimate`
 *   was set.
 */
@Serializable
public data class CompactionStats(

  /**
   * Whether a summary was actually produced. False means the payload passed through untouched
   * (nothing older than the kept tail, or no summarizable text).
   */
  @SerialName(value = "compacted") val compacted: Boolean,

  /**
   * Number of transcript chunks summarized in the map phase. 0 on passthrough, 1 when the
   * transcript fit in a single summarizer call.
   */
  @SerialName(value = "chunksProcessed") val chunksProcessed: Int? = null,

  /**
   * Number of summarizer rounds. 1 for a direct summary; 2+ when chunk summaries had to be merged
   * (map-reduce).
   */
  @SerialName(value = "passes") val passes: Int? = null,

  /**
   * Provider-reported input tokens summed across every summarizer call. Null when the provider
   * reported no usage metadata.
   */
  @SerialName(value = "summarizerPromptTokens") val summarizerPromptTokens: Int? = null,

  /**
   * Provider-reported output tokens summed across every summarizer call. Null when the provider
   * reported no usage metadata.
   */
  @SerialName(value = "summarizerCompletionTokens") val summarizerCompletionTokens: Int? = null,

  /** The requested output budget, echoed back when `targetTokensEstimate` was set. */
  @SerialName(value = "targetTokensEstimate") val targetTokensEstimate: Int? = null,
) {}

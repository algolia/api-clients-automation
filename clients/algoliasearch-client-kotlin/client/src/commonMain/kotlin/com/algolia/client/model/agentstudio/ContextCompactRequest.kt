/**
 * Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be
 * lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
 */
package com.algolia.client.model.agentstudio

import kotlinx.serialization.*
import kotlinx.serialization.json.*

/**
 * LLM-summarization request. The caller's provider runs the summary, so cost is theirs.
 *
 * @param providerID Provider UUID for LLM credentials.
 * @param model Model name (e.g., gpt-4o-mini).
 * @param messages
 * @param keepLastMessages Number of trailing messages kept verbatim; everything older is
 *   summarized.
 * @param instructions Extra guidance for the summarizer (e.g. 'keep every product reference').
 *   Appended inside the server-owned prompt frame, so it steers the summary without the model
 *   echoing your wording back into it.
 * @param targetTokensEstimate Desired size of the final summary, in tokens. Translated into
 *   word-count guidance for the summarizer (best-effort, not a hard cap). Omit to let the
 *   summarizer default to a concise summary.
 */
@Serializable
public data class ContextCompactRequest(

  /** Provider UUID for LLM credentials. */
  @SerialName(value = "providerID") val providerID: String,

  /** Model name (e.g., gpt-4o-mini). */
  @SerialName(value = "model") val model: String,
  @SerialName(value = "messages") val messages: MessagesUnion,

  /** Number of trailing messages kept verbatim; everything older is summarized. */
  @SerialName(value = "keepLastMessages") val keepLastMessages: Int? = null,

  /**
   * Extra guidance for the summarizer (e.g. 'keep every product reference'). Appended inside the
   * server-owned prompt frame, so it steers the summary without the model echoing your wording back
   * into it.
   */
  @SerialName(value = "instructions") val instructions: String? = null,

  /**
   * Desired size of the final summary, in tokens. Translated into word-count guidance for the
   * summarizer (best-effort, not a hard cap). Omit to let the summarizer default to a concise
   * summary.
   */
  @SerialName(value = "targetTokensEstimate") val targetTokensEstimate: Int? = null,
) {}

// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

/// Strict request body with a task selector, `input`, and dashboard-only `configuration`.  Unknown top-level fields are
/// rejected so clients cannot pass trusted configuration fields (prompts, schemas, providers, models, generation
/// settings) at runtime. `configuration` is only accepted for `agentId=test` dashboard requests and is rejected for
/// real agents.
public struct TaskRequest: Codable, JSONEncodable {
    /// ID of the configured task to run.
    public var task: String?
    public var kind: TaskKind?
    /// Arbitrary JSON input passed to the task as data.
    public var input: [String: AnyCodable]
    public var configuration: AgentTestConfiguration?

    public init(
        task: String? = nil,
        kind: TaskKind? = nil,
        input: [String: AnyCodable],
        configuration: AgentTestConfiguration? = nil
    ) {
        self.task = task
        self.kind = kind
        self.input = input
        self.configuration = configuration
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case task
        case kind
        case input
        case configuration
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.task, forKey: .task)
        try container.encodeIfPresent(self.kind, forKey: .kind)
        try container.encode(self.input, forKey: .input)
        try container.encodeIfPresent(self.configuration, forKey: .configuration)
    }
}

extension TaskRequest: Equatable {}

extension TaskRequest: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.task?.hashValue)
        hasher.combine(self.kind?.hashValue)
        hasher.combine(self.input.hashValue)
        hasher.combine(self.configuration?.hashValue)
    }
}

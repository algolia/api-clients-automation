// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

/// Deterministic trim request (no LLM calls).
public struct ContextTrimRequest: Codable, JSONEncodable {
    public var messages: MessagesUnion
    /// Keep only the last N messages, dropping older ones. Applied before the token budget.
    public var keepLastMessages: Int?
    /// Drop oldest messages until the estimated token total fits this budget (heuristic).
    public var maxTokensEstimate: Int?
    /// Strip tool-invocation/result parts from the messages that are kept. Applied before the token budget, so the
    /// budget counts only surviving content.
    public var dropToolParts: Bool?

    public init(
        messages: MessagesUnion,
        keepLastMessages: Int? = nil,
        maxTokensEstimate: Int? = nil,
        dropToolParts: Bool? = nil
    ) {
        self.messages = messages
        self.keepLastMessages = keepLastMessages
        self.maxTokensEstimate = maxTokensEstimate
        self.dropToolParts = dropToolParts
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case messages
        case keepLastMessages
        case maxTokensEstimate
        case dropToolParts
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.messages, forKey: .messages)
        try container.encodeIfPresent(self.keepLastMessages, forKey: .keepLastMessages)
        try container.encodeIfPresent(self.maxTokensEstimate, forKey: .maxTokensEstimate)
        try container.encodeIfPresent(self.dropToolParts, forKey: .dropToolParts)
    }
}

extension ContextTrimRequest: Equatable {}

extension ContextTrimRequest: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.messages.hashValue)
        hasher.combine(self.keepLastMessages?.hashValue)
        hasher.combine(self.maxTokensEstimate?.hashValue)
        hasher.combine(self.dropToolParts?.hashValue)
    }
}

// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

/// Before/after accounting for a context operation.  ``tokens*Estimate`` fields are heuristic (tiktoken BPE over the
/// serialized messages), not an authoritative provider count. ``messages_dropped`` is the net reduction in message
/// count (``messages_before - messages_after``); for compaction the summarized tail is replaced by a single summary
/// message, so this reflects the net change rather than a raw delete count.
public struct ContextStats: Codable, JSONEncodable {
    /// Message count before the operation.
    public var messagesBefore: Int
    /// Message count after the operation.
    public var messagesAfter: Int
    /// Estimated token count before the operation.
    public var tokensBeforeEstimate: Int
    /// Estimated token count after the operation.
    public var tokensAfterEstimate: Int
    /// Net reduction in message count (before - after).
    public var messagesDropped: Int

    public init(
        messagesBefore: Int,
        messagesAfter: Int,
        tokensBeforeEstimate: Int,
        tokensAfterEstimate: Int,
        messagesDropped: Int
    ) {
        self.messagesBefore = messagesBefore
        self.messagesAfter = messagesAfter
        self.tokensBeforeEstimate = tokensBeforeEstimate
        self.tokensAfterEstimate = tokensAfterEstimate
        self.messagesDropped = messagesDropped
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case messagesBefore
        case messagesAfter
        case tokensBeforeEstimate
        case tokensAfterEstimate
        case messagesDropped
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.messagesBefore, forKey: .messagesBefore)
        try container.encode(self.messagesAfter, forKey: .messagesAfter)
        try container.encode(self.tokensBeforeEstimate, forKey: .tokensBeforeEstimate)
        try container.encode(self.tokensAfterEstimate, forKey: .tokensAfterEstimate)
        try container.encode(self.messagesDropped, forKey: .messagesDropped)
    }
}

extension ContextStats: Equatable {}

extension ContextStats: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.messagesBefore.hashValue)
        hasher.combine(self.messagesAfter.hashValue)
        hasher.combine(self.tokensBeforeEstimate.hashValue)
        hasher.combine(self.tokensAfterEstimate.hashValue)
        hasher.combine(self.messagesDropped.hashValue)
    }
}

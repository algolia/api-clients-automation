// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

/// LLM-summarization request. The caller's provider runs the summary, so cost is theirs.
public struct ContextCompactRequest: Codable, JSONEncodable {
    /// Provider UUID for LLM credentials.
    public var providerID: String
    /// Model name (e.g., gpt-4o-mini).
    public var model: String
    public var messages: MessagesUnion
    /// Number of trailing messages kept verbatim; everything older is summarized.
    public var keepLastMessages: Int?
    /// Extra guidance for the summarizer (e.g. 'keep every product reference'). Appended inside the server-owned prompt
    /// frame, so it steers the summary without the model echoing your wording back into it.
    public var instructions: String?
    /// Desired size of the final summary, in tokens. Translated into word-count guidance for the summarizer
    /// (best-effort, not a hard cap). Omit to let the summarizer default to a concise summary.
    public var targetTokensEstimate: Int?

    public init(
        providerID: String,
        model: String,
        messages: MessagesUnion,
        keepLastMessages: Int? = nil,
        instructions: String? = nil,
        targetTokensEstimate: Int? = nil
    ) {
        self.providerID = providerID
        self.model = model
        self.messages = messages
        self.keepLastMessages = keepLastMessages
        self.instructions = instructions
        self.targetTokensEstimate = targetTokensEstimate
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case providerID
        case model
        case messages
        case keepLastMessages
        case instructions
        case targetTokensEstimate
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.providerID, forKey: .providerID)
        try container.encode(self.model, forKey: .model)
        try container.encode(self.messages, forKey: .messages)
        try container.encodeIfPresent(self.keepLastMessages, forKey: .keepLastMessages)
        try container.encodeIfPresent(self.instructions, forKey: .instructions)
        try container.encodeIfPresent(self.targetTokensEstimate, forKey: .targetTokensEstimate)
    }
}

extension ContextCompactRequest: Equatable {}

extension ContextCompactRequest: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(self.providerID.hashValue)
        hasher.combine(self.model.hashValue)
        hasher.combine(self.messages.hashValue)
        hasher.combine(self.keepLastMessages?.hashValue)
        hasher.combine(self.instructions?.hashValue)
        hasher.combine(self.targetTokensEstimate?.hashValue)
    }
}

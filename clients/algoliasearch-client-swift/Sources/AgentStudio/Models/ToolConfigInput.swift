// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import Foundation
#if canImport(AlgoliaCore)
    import AlgoliaCore
#endif

public enum ToolConfigInput: Codable, JSONEncodable, AbstractEncodable {
    case clientSideToolConfig(ClientSideToolConfig)
    case mcpServerToolConfigInput(McpServerToolConfigInput)
    case algoliaSearchToolConfigInput(AlgoliaSearchToolConfigInput)
    case algoliaRecommendToolConfigInput(AlgoliaRecommendToolConfigInput)
    case algoliaDisplayResultsToolConfig(AlgoliaDisplayResultsToolConfig)
    case unknownToolConfig(UnknownToolConfig)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case let .clientSideToolConfig(value):
            try container.encode(value)
        case let .mcpServerToolConfigInput(value):
            try container.encode(value)
        case let .algoliaSearchToolConfigInput(value):
            try container.encode(value)
        case let .algoliaRecommendToolConfigInput(value):
            try container.encode(value)
        case let .algoliaDisplayResultsToolConfig(value):
            try container.encode(value)
        case let .unknownToolConfig(value):
            try container.encode(value)
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let jsonObject = try? container.decode([String: AnyCodable].self),
           let discriminatorValue = jsonObject["type"]?.value as? String {
            switch discriminatorValue {
            case "algolia_display_results":
                self = try .algoliaDisplayResultsToolConfig(container.decode(AlgoliaDisplayResultsToolConfig.self))
                return
            case "algolia_recommend":
                self = try .algoliaRecommendToolConfigInput(container.decode(AlgoliaRecommendToolConfigInput.self))
                return
            case "algolia_search_index":
                self = try .algoliaSearchToolConfigInput(container.decode(AlgoliaSearchToolConfigInput.self))
                return
            case "client_side":
                self = try .clientSideToolConfig(container.decode(ClientSideToolConfig.self))
                return
            case "mcp_tools":
                self = try .mcpServerToolConfigInput(container.decode(McpServerToolConfigInput.self))
                return
            case "unknown":
                self = try .unknownToolConfig(container.decode(UnknownToolConfig.self))
                return
            default:
                break
            }
        }
        if let value = try? container.decode(ClientSideToolConfig.self) {
            self = .clientSideToolConfig(value)
        } else if let value = try? container.decode(McpServerToolConfigInput.self) {
            self = .mcpServerToolConfigInput(value)
        } else if let value = try? container.decode(AlgoliaSearchToolConfigInput.self) {
            self = .algoliaSearchToolConfigInput(value)
        } else if let value = try? container.decode(AlgoliaRecommendToolConfigInput.self) {
            self = .algoliaRecommendToolConfigInput(value)
        } else if let value = try? container.decode(AlgoliaDisplayResultsToolConfig.self) {
            self = .algoliaDisplayResultsToolConfig(value)
        } else if let value = try? container.decode(UnknownToolConfig.self) {
            self = .unknownToolConfig(value)
        } else {
            throw DecodingError.typeMismatch(
                Self.Type.self,
                .init(codingPath: decoder.codingPath, debugDescription: "Unable to decode instance of ToolConfigInput")
            )
        }
    }

    public func GetActualInstance() -> Encodable {
        switch self {
        case let .clientSideToolConfig(value):
            value as ClientSideToolConfig
        case let .mcpServerToolConfigInput(value):
            value as McpServerToolConfigInput
        case let .algoliaSearchToolConfigInput(value):
            value as AlgoliaSearchToolConfigInput
        case let .algoliaRecommendToolConfigInput(value):
            value as AlgoliaRecommendToolConfigInput
        case let .algoliaDisplayResultsToolConfig(value):
            value as AlgoliaDisplayResultsToolConfig
        case let .unknownToolConfig(value):
            value as UnknownToolConfig
        }
    }
}

extension ToolConfigInput: Equatable {}
extension ToolConfigInput: Hashable {}

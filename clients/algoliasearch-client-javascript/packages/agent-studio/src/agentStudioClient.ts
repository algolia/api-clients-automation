// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.

import type {
  CreateClientOptions,
  Headers,
  Host,
  QueryParameters,
  Request,
  RequestOptions,
} from '@algolia/client-common';
import { createAuth, createTransporter, getAlgoliaAgent } from '@algolia/client-common';

import type { AgentCompletionResponse } from '../model/agentCompletionResponse';
import type { AgentConfigCreate } from '../model/agentConfigCreate';
import type { AgentWithVersionResponse } from '../model/agentWithVersionResponse';
import type { ApplicationConfigPatch } from '../model/applicationConfigPatch';
import type { ApplicationConfigResponse } from '../model/applicationConfigResponse';
import type { ConversationFullResponse } from '../model/conversationFullResponse';
import type { PaginatedAgentsResponse } from '../model/paginatedAgentsResponse';
import type { PaginatedConversationsResponse } from '../model/paginatedConversationsResponse';
import type { PaginatedProviderAuthenticationsResponse } from '../model/paginatedProviderAuthenticationsResponse';
import type { ProviderAuthenticationCreate } from '../model/providerAuthenticationCreate';
import type { ProviderAuthenticationResponse } from '../model/providerAuthenticationResponse';
import type { UserDataResponse } from '../model/userDataResponse';

import type {
  CreateAgentCompletionProps,
  CustomDeleteProps,
  CustomGetProps,
  CustomPostProps,
  CustomPutProps,
  DeleteAgentConversationsProps,
  DeleteAgentProps,
  DeleteConversationProps,
  DeleteProviderProps,
  DeleteUserDataProps,
  ExportConversationsProps,
  GetAgentProps,
  GetConversationProps,
  GetProviderProps,
  GetUserDataProps,
  InvalidateAgentCacheProps,
  ListAgentConversationsProps,
  ListAgentsProps,
  ListProviderModelsProps,
  ListProvidersProps,
  PublishAgentProps,
  UnpublishAgentProps,
  UpdateAgentProps,
  UpdateProviderProps,
} from '../model/clientMethodProps';

export const apiClientVersion = '0.0.1-alpha.0';

export const REGIONS = ['eu', 'us'] as const;
export type Region = (typeof REGIONS)[number];
export type RegionOptions = { region: Region };

function getDefaultHosts(region: Region): Host[] {
  const url = 'agent-studio.{region}.algolia.com'.replace('{region}', region);

  return [{ url, accept: 'readWrite', protocol: 'https' }];
}

export function createAgentStudioClient({
  appId: appIdOption,
  apiKey: apiKeyOption,
  authMode,
  algoliaAgents,
  region: regionOption,
  ...options
}: CreateClientOptions & RegionOptions) {
  const auth = createAuth(appIdOption, apiKeyOption, authMode);
  const transporter = createTransporter({
    hosts: getDefaultHosts(regionOption),
    ...options,
    algoliaAgent: getAlgoliaAgent({
      algoliaAgents,
      client: 'AgentStudio',
      version: apiClientVersion,
    }),
    baseHeaders: {
      'content-type': 'text/plain',
      ...auth.headers(),
      ...options.baseHeaders,
    },
    baseQueryParameters: {
      ...auth.queryParameters(),
      ...options.baseQueryParameters,
    },
  });

  return {
    transporter,

    /**
     * The `appId` currently in use.
     */
    appId: appIdOption,

    /**
     * The `apiKey` currently in use.
     */
    apiKey: apiKeyOption,

    /**
     * Clears the cache of the transporter for the `requestsCache` and `responsesCache` properties.
     */
    clearCache(): Promise<void> {
      return Promise.all([transporter.requestsCache.clear(), transporter.responsesCache.clear()]).then(() => undefined);
    },

    /**
     * Get the value of the `algoliaAgent`, used by our libraries internally and telemetry system.
     */
    get _ua(): string {
      return transporter.algoliaAgent.value;
    },

    /**
     * Adds a `segment` to the `x-algolia-agent` sent with every requests.
     *
     * @param segment - The algolia agent (user-agent) segment to add.
     * @param version - The version of the agent.
     */
    addAlgoliaAgent(segment: string, version?: string | undefined): void {
      transporter.algoliaAgent.add({ segment, version });
    },

    /**
     * Helper method to switch the API key used to authenticate the requests.
     *
     * @param params - Method params.
     * @param params.apiKey - The new API Key to use.
     */
    setClientApiKey({ apiKey }: { apiKey: string }): void {
      if (!authMode || authMode === 'WithinHeaders') {
        transporter.baseHeaders['x-algolia-api-key'] = apiKey;
      } else {
        transporter.baseQueryParameters['x-algolia-api-key'] = apiKey;
      }
    },

    /**
     * Create a new agent.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param agentConfigCreate - The agentConfigCreate object.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    createAgent(
      agentConfigCreate: AgentConfigCreate,
      requestOptions?: RequestOptions,
    ): Promise<AgentWithVersionResponse> {
      if (!agentConfigCreate) {
        throw new Error('Parameter `agentConfigCreate` is required when calling `createAgent`.');
      }

      if (!agentConfigCreate.name) {
        throw new Error('Parameter `agentConfigCreate.name` is required when calling `createAgent`.');
      }
      if (!agentConfigCreate.instructions) {
        throw new Error('Parameter `agentConfigCreate.instructions` is required when calling `createAgent`.');
      }

      const requestPath = '/1/agents';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'POST',
        path: requestPath,
        queryParameters,
        headers,
        data: agentConfigCreate,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Create a completion for the specified agent.
     *
     * Required API Key ACLs:
     *  - search
     * @param createAgentCompletion - The createAgentCompletion object.
     * @param createAgentCompletion.agentId - The agentId.
     * @param createAgentCompletion.compatibilityMode - Compatibility mode for the completion API.
     * @param createAgentCompletion.agentCompletionRequest - The agentCompletionRequest object.
     * @param createAgentCompletion.stream - Whether to stream the response or not.
     * @param createAgentCompletion.cache - Use cached responses if available.
     * @param createAgentCompletion.memory - Whether to use memory or not.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    createAgentCompletion(
      { agentId, compatibilityMode, agentCompletionRequest, stream, cache, memory }: CreateAgentCompletionProps,
      requestOptions?: RequestOptions,
    ): Promise<AgentCompletionResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `createAgentCompletion`.');
      }

      if (!compatibilityMode) {
        throw new Error('Parameter `compatibilityMode` is required when calling `createAgentCompletion`.');
      }

      if (!agentCompletionRequest) {
        throw new Error('Parameter `agentCompletionRequest` is required when calling `createAgentCompletion`.');
      }

      if (!agentCompletionRequest.messages) {
        throw new Error(
          'Parameter `agentCompletionRequest.messages` is required when calling `createAgentCompletion`.',
        );
      }

      const requestPath = '/1/agents/{agentId}/completions'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (compatibilityMode !== undefined) {
        queryParameters['compatibilityMode'] = compatibilityMode.toString();
      }

      if (stream !== undefined) {
        queryParameters['stream'] = stream.toString();
      }

      if (cache !== undefined) {
        queryParameters['cache'] = cache.toString();
      }

      if (memory !== undefined) {
        queryParameters['memory'] = memory.toString();
      }

      const request: Request = {
        method: 'POST',
        path: requestPath,
        queryParameters,
        headers,
        data: agentCompletionRequest,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Create Provider.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param providerAuthenticationCreate - The providerAuthenticationCreate object.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    createProvider(
      providerAuthenticationCreate: ProviderAuthenticationCreate,
      requestOptions?: RequestOptions,
    ): Promise<ProviderAuthenticationResponse> {
      if (!providerAuthenticationCreate) {
        throw new Error('Parameter `providerAuthenticationCreate` is required when calling `createProvider`.');
      }

      if (!providerAuthenticationCreate.name) {
        throw new Error('Parameter `providerAuthenticationCreate.name` is required when calling `createProvider`.');
      }
      if (!providerAuthenticationCreate.providerName) {
        throw new Error(
          'Parameter `providerAuthenticationCreate.providerName` is required when calling `createProvider`.',
        );
      }
      if (!providerAuthenticationCreate.input) {
        throw new Error('Parameter `providerAuthenticationCreate.input` is required when calling `createProvider`.');
      }

      const requestPath = '/1/providers';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'POST',
        path: requestPath,
        queryParameters,
        headers,
        data: providerAuthenticationCreate,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * This method lets you send requests to the Algolia REST API.
     * @param customDelete - The customDelete object.
     * @param customDelete.path - Path of the endpoint, for example `1/newFeature`.
     * @param customDelete.parameters - Query parameters to apply to the current query.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    customDelete(
      { path, parameters }: CustomDeleteProps,
      requestOptions?: RequestOptions,
    ): Promise<Record<string, unknown>> {
      if (!path) {
        throw new Error('Parameter `path` is required when calling `customDelete`.');
      }

      const requestPath = '/{path}'.replace('{path}', path);
      const headers: Headers = {};
      const queryParameters: QueryParameters = parameters ? parameters : {};

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * This method lets you send requests to the Algolia REST API.
     * @param customGet - The customGet object.
     * @param customGet.path - Path of the endpoint, for example `1/newFeature`.
     * @param customGet.parameters - Query parameters to apply to the current query.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    customGet({ path, parameters }: CustomGetProps, requestOptions?: RequestOptions): Promise<Record<string, unknown>> {
      if (!path) {
        throw new Error('Parameter `path` is required when calling `customGet`.');
      }

      const requestPath = '/{path}'.replace('{path}', path);
      const headers: Headers = {};
      const queryParameters: QueryParameters = parameters ? parameters : {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * This method lets you send requests to the Algolia REST API.
     * @param customPost - The customPost object.
     * @param customPost.path - Path of the endpoint, for example `1/newFeature`.
     * @param customPost.parameters - Query parameters to apply to the current query.
     * @param customPost.body - Parameters to send with the custom request.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    customPost(
      { path, parameters, body }: CustomPostProps,
      requestOptions?: RequestOptions,
    ): Promise<Record<string, unknown>> {
      if (!path) {
        throw new Error('Parameter `path` is required when calling `customPost`.');
      }

      const requestPath = '/{path}'.replace('{path}', path);
      const headers: Headers = {};
      const queryParameters: QueryParameters = parameters ? parameters : {};

      const request: Request = {
        method: 'POST',
        path: requestPath,
        queryParameters,
        headers,
        data: body ? body : {},
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * This method lets you send requests to the Algolia REST API.
     * @param customPut - The customPut object.
     * @param customPut.path - Path of the endpoint, for example `1/newFeature`.
     * @param customPut.parameters - Query parameters to apply to the current query.
     * @param customPut.body - Parameters to send with the custom request.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    customPut(
      { path, parameters, body }: CustomPutProps,
      requestOptions?: RequestOptions,
    ): Promise<Record<string, unknown>> {
      if (!path) {
        throw new Error('Parameter `path` is required when calling `customPut`.');
      }

      const requestPath = '/{path}'.replace('{path}', path);
      const headers: Headers = {};
      const queryParameters: QueryParameters = parameters ? parameters : {};

      const request: Request = {
        method: 'PUT',
        path: requestPath,
        queryParameters,
        headers,
        data: body ? body : {},
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Delete the specified agent.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param deleteAgent - The deleteAgent object.
     * @param deleteAgent.agentId - The agentId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    deleteAgent({ agentId }: DeleteAgentProps, requestOptions?: RequestOptions): Promise<void> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `deleteAgent`.');
      }

      const requestPath = '/1/agents/{agentId}'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Deletes the conversations matching the given filers.
     *
     * Required API Key ACLs:
     *  - logs
     * @param deleteAgentConversations - The deleteAgentConversations object.
     * @param deleteAgentConversations.agentId - The agentId.
     * @param deleteAgentConversations.startDate - Filter conversations created after this date (format: YYYY-MM-DD).
     * @param deleteAgentConversations.endDate - Filter conversations created before this date (format: YYYY-MM-DD).
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    deleteAgentConversations(
      { agentId, startDate, endDate }: DeleteAgentConversationsProps,
      requestOptions?: RequestOptions,
    ): Promise<void> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `deleteAgentConversations`.');
      }

      const requestPath = '/1/agents/{agentId}/conversations'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (startDate !== undefined) {
        queryParameters['startDate'] = startDate.toString();
      }

      if (endDate !== undefined) {
        queryParameters['endDate'] = endDate.toString();
      }

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Deletes the conversation with the given ID.
     *
     * Required API Key ACLs:
     *  - logs
     * @param deleteConversation - The deleteConversation object.
     * @param deleteConversation.agentId - The agentId.
     * @param deleteConversation.conversationId - The conversationId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    deleteConversation(
      { agentId, conversationId }: DeleteConversationProps,
      requestOptions?: RequestOptions,
    ): Promise<void> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `deleteConversation`.');
      }

      if (!conversationId) {
        throw new Error('Parameter `conversationId` is required when calling `deleteConversation`.');
      }

      const requestPath = '/1/agents/{agentId}/conversations/{conversationId}'
        .replace('{agentId}', encodeURIComponent(agentId))
        .replace('{conversationId}', encodeURIComponent(conversationId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Delete Provider.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param deleteProvider - The deleteProvider object.
     * @param deleteProvider.providerId - The providerId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    deleteProvider({ providerId }: DeleteProviderProps, requestOptions?: RequestOptions): Promise<void> {
      if (!providerId) {
        throw new Error('Parameter `providerId` is required when calling `deleteProvider`.');
      }

      const requestPath = '/1/providers/{providerId}'.replace('{providerId}', encodeURIComponent(providerId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Permanently deletes all messages for the given user token. Does not delete conversations.
     *
     * Required API Key ACLs:
     *  - logs
     * @param deleteUserData - The deleteUserData object.
     * @param deleteUserData.userToken - The userToken.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    deleteUserData({ userToken }: DeleteUserDataProps, requestOptions?: RequestOptions): Promise<void> {
      if (!userToken) {
        throw new Error('Parameter `userToken` is required when calling `deleteUserData`.');
      }

      const requestPath = '/1/user-data/{userToken}'.replace('{userToken}', encodeURIComponent(userToken));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Exports all conversations based on the passed filters.
     *
     * Required API Key ACLs:
     *  - logs
     * @param exportConversations - The exportConversations object.
     * @param exportConversations.agentId - The agentId.
     * @param exportConversations.startDate - Filter conversations created after this date (format: YYYY-MM-DD).
     * @param exportConversations.endDate - Filter conversations created before this date (format: YYYY-MM-DD).
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    exportConversations(
      { agentId, startDate, endDate }: ExportConversationsProps,
      requestOptions?: RequestOptions,
    ): Promise<Array<ConversationFullResponse>> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `exportConversations`.');
      }

      const requestPath = '/1/agents/{agentId}/conversations/export'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (startDate !== undefined) {
        queryParameters['startDate'] = startDate.toString();
      }

      if (endDate !== undefined) {
        queryParameters['endDate'] = endDate.toString();
      }

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Retrieve details of the specified agent.
     *
     * Required API Key ACLs:
     *  - settings
     * @param getAgent - The getAgent object.
     * @param getAgent.agentId - The agentId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    getAgent({ agentId }: GetAgentProps, requestOptions?: RequestOptions): Promise<AgentWithVersionResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `getAgent`.');
      }

      const requestPath = '/1/agents/{agentId}'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Get Configuration.
     *
     * Required API Key ACLs:
     *  - logs
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    getApplicationConfiguration(requestOptions?: RequestOptions | undefined): Promise<ApplicationConfigResponse> {
      const requestPath = '/1/configuration';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Retrieves the conversation and its messages for the given ID.
     *
     * Required API Key ACLs:
     *  - logs
     * @param getConversation - The getConversation object.
     * @param getConversation.agentId - The agentId.
     * @param getConversation.conversationId - The conversationId.
     * @param getConversation.xAlgoliaSecureUserToken - The X-Algolia-Secure-User-Token.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    getConversation(
      { agentId, conversationId, xAlgoliaSecureUserToken }: GetConversationProps,
      requestOptions?: RequestOptions,
    ): Promise<ConversationFullResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `getConversation`.');
      }

      if (!conversationId) {
        throw new Error('Parameter `conversationId` is required when calling `getConversation`.');
      }

      const requestPath = '/1/agents/{agentId}/conversations/{conversationId}'
        .replace('{agentId}', encodeURIComponent(agentId))
        .replace('{conversationId}', encodeURIComponent(conversationId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (xAlgoliaSecureUserToken !== undefined) {
        headers['X-Algolia-Secure-User-Token'] = xAlgoliaSecureUserToken.toString();
      }

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Get Provider.
     *
     * Required API Key ACLs:
     *  - settings
     * @param getProvider - The getProvider object.
     * @param getProvider.providerId - The providerId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    getProvider(
      { providerId }: GetProviderProps,
      requestOptions?: RequestOptions,
    ): Promise<ProviderAuthenticationResponse> {
      if (!providerId) {
        throw new Error('Parameter `providerId` is required when calling `getProvider`.');
      }

      const requestPath = '/1/providers/{providerId}'.replace('{providerId}', encodeURIComponent(providerId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Retrieves all memories, conversations and their messages for the given user token.
     *
     * Required API Key ACLs:
     *  - logs
     * @param getUserData - The getUserData object.
     * @param getUserData.userToken - The userToken.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    getUserData({ userToken }: GetUserDataProps, requestOptions?: RequestOptions): Promise<UserDataResponse> {
      if (!userToken) {
        throw new Error('Parameter `userToken` is required when calling `getUserData`.');
      }

      const requestPath = '/1/user-data/{userToken}'.replace('{userToken}', encodeURIComponent(userToken));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Invalidate cached completions for this agent. Filter with `before` (exclusive).
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param invalidateAgentCache - The invalidateAgentCache object.
     * @param invalidateAgentCache.agentId - The agentId.
     * @param invalidateAgentCache.before - Delete entries strictly before this date (exclusive, YYYY-MM-DD).
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    invalidateAgentCache(
      { agentId, before }: InvalidateAgentCacheProps,
      requestOptions?: RequestOptions,
    ): Promise<void> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `invalidateAgentCache`.');
      }

      const requestPath = '/1/agents/{agentId}/cache'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (before !== undefined) {
        queryParameters['before'] = before.toString();
      }

      const request: Request = {
        method: 'DELETE',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Retrieves the conversations for the given agent ID.
     *
     * Required API Key ACLs:
     *  - logs
     * @param listAgentConversations - The listAgentConversations object.
     * @param listAgentConversations.agentId - The agentId.
     * @param listAgentConversations.startDate - Filter conversations created after this date (format: YYYY-MM-DD).
     * @param listAgentConversations.endDate - Filter conversations created before this date (format: YYYY-MM-DD).
     * @param listAgentConversations.page - Page number.
     * @param listAgentConversations.limit - Items per page.
     * @param listAgentConversations.xAlgoliaSecureUserToken - The X-Algolia-Secure-User-Token.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    listAgentConversations(
      { agentId, startDate, endDate, page, limit, xAlgoliaSecureUserToken }: ListAgentConversationsProps,
      requestOptions?: RequestOptions,
    ): Promise<PaginatedConversationsResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `listAgentConversations`.');
      }

      const requestPath = '/1/agents/{agentId}/conversations'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (startDate !== undefined) {
        queryParameters['startDate'] = startDate.toString();
      }

      if (endDate !== undefined) {
        queryParameters['endDate'] = endDate.toString();
      }

      if (page !== undefined) {
        queryParameters['page'] = page.toString();
      }

      if (limit !== undefined) {
        queryParameters['limit'] = limit.toString();
      }

      if (xAlgoliaSecureUserToken !== undefined) {
        headers['X-Algolia-Secure-User-Token'] = xAlgoliaSecureUserToken.toString();
      }

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * List all agents with pagination and filtering.
     *
     * Required API Key ACLs:
     *  - settings
     * @param listAgents - The listAgents object.
     * @param listAgents.page - Page number.
     * @param listAgents.limit - Items per page.
     * @param listAgents.providerId - Filter by provider id.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    listAgents(
      { page, limit, providerId }: ListAgentsProps = {},
      requestOptions: RequestOptions | undefined = undefined,
    ): Promise<PaginatedAgentsResponse> {
      const requestPath = '/1/agents';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (page !== undefined) {
        queryParameters['page'] = page.toString();
      }

      if (limit !== undefined) {
        queryParameters['limit'] = limit.toString();
      }

      if (providerId !== undefined) {
        queryParameters['providerId'] = providerId.toString();
      }

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Get Provider Models.
     *
     * Required API Key ACLs:
     *  - settings
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    listModels(requestOptions?: RequestOptions | undefined): Promise<{ [key: string]: Array<string> }> {
      const requestPath = '/1/providers/models';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Get available models for a specific provider.
     *
     * Required API Key ACLs:
     *  - settings
     * @param listProviderModels - The listProviderModels object.
     * @param listProviderModels.providerId - The providerId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    listProviderModels(
      { providerId }: ListProviderModelsProps,
      requestOptions?: RequestOptions,
    ): Promise<Array<string>> {
      if (!providerId) {
        throw new Error('Parameter `providerId` is required when calling `listProviderModels`.');
      }

      const requestPath = '/1/providers/{providerId}/models'.replace('{providerId}', encodeURIComponent(providerId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * List Providers.
     *
     * Required API Key ACLs:
     *  - settings
     * @param listProviders - The listProviders object.
     * @param listProviders.page - Page number.
     * @param listProviders.limit - Items per page.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    listProviders(
      { page, limit }: ListProvidersProps = {},
      requestOptions: RequestOptions | undefined = undefined,
    ): Promise<PaginatedProviderAuthenticationsResponse> {
      const requestPath = '/1/providers';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      if (page !== undefined) {
        queryParameters['page'] = page.toString();
      }

      if (limit !== undefined) {
        queryParameters['limit'] = limit.toString();
      }

      const request: Request = {
        method: 'GET',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Publish the specified agent.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param publishAgent - The publishAgent object.
     * @param publishAgent.agentId - The agentId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    publishAgent({ agentId }: PublishAgentProps, requestOptions?: RequestOptions): Promise<AgentWithVersionResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `publishAgent`.');
      }

      const requestPath = '/1/agents/{agentId}/publish'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'POST',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Unpublish the specified agent.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param unpublishAgent - The unpublishAgent object.
     * @param unpublishAgent.agentId - The agentId.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    unpublishAgent(
      { agentId }: UnpublishAgentProps,
      requestOptions?: RequestOptions,
    ): Promise<AgentWithVersionResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `unpublishAgent`.');
      }

      const requestPath = '/1/agents/{agentId}/unpublish'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'POST',
        path: requestPath,
        queryParameters,
        headers,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Update the specified agent.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param updateAgent - The updateAgent object.
     * @param updateAgent.agentId - The agentId.
     * @param updateAgent.agentConfigUpdate - The agentConfigUpdate object.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    updateAgent(
      { agentId, agentConfigUpdate }: UpdateAgentProps,
      requestOptions?: RequestOptions,
    ): Promise<AgentWithVersionResponse> {
      if (!agentId) {
        throw new Error('Parameter `agentId` is required when calling `updateAgent`.');
      }

      if (!agentConfigUpdate) {
        throw new Error('Parameter `agentConfigUpdate` is required when calling `updateAgent`.');
      }

      const requestPath = '/1/agents/{agentId}'.replace('{agentId}', encodeURIComponent(agentId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'PATCH',
        path: requestPath,
        queryParameters,
        headers,
        data: agentConfigUpdate,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Patch Configuration.
     *
     * Required API Key ACLs:
     *  - logs
     * @param applicationConfigPatch - The applicationConfigPatch object.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    updateConfiguration(
      applicationConfigPatch: ApplicationConfigPatch,
      requestOptions?: RequestOptions,
    ): Promise<ApplicationConfigResponse> {
      if (!applicationConfigPatch) {
        throw new Error('Parameter `applicationConfigPatch` is required when calling `updateConfiguration`.');
      }

      const requestPath = '/1/configuration';
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'PATCH',
        path: requestPath,
        queryParameters,
        headers,
        data: applicationConfigPatch,
      };

      return transporter.request(request, requestOptions);
    },

    /**
     * Update Provider.
     *
     * Required API Key ACLs:
     *  - editSettings
     * @param updateProvider - The updateProvider object.
     * @param updateProvider.providerId - The providerId.
     * @param updateProvider.providerAuthenticationPatch - The providerAuthenticationPatch object.
     * @param requestOptions - The requestOptions to send along with the query, they will be merged with the transporter requestOptions.
     */
    updateProvider(
      { providerId, providerAuthenticationPatch }: UpdateProviderProps,
      requestOptions?: RequestOptions,
    ): Promise<ProviderAuthenticationResponse> {
      if (!providerId) {
        throw new Error('Parameter `providerId` is required when calling `updateProvider`.');
      }

      if (!providerAuthenticationPatch) {
        throw new Error('Parameter `providerAuthenticationPatch` is required when calling `updateProvider`.');
      }

      const requestPath = '/1/providers/{providerId}'.replace('{providerId}', encodeURIComponent(providerId));
      const headers: Headers = {};
      const queryParameters: QueryParameters = {};

      const request: Request = {
        method: 'PATCH',
        path: requestPath,
        queryParameters,
        headers,
        data: providerAuthenticationPatch,
      };

      return transporter.request(request, requestOptions);
    },
  };
}

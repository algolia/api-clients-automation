# coding: utf-8

"""
Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
"""

from __future__ import annotations

from json import loads
from sys import version_info
from typing import Any, Dict, List, Optional

from pydantic import BaseModel, ConfigDict

if version_info >= (3, 11):
    from typing import Self
else:
    from typing_extensions import Self


from algoliasearch.agent_studio.models.message_part_assistant_message_agui import (
    MessagePartAssistantMessageAGUI,
)
from algoliasearch.agent_studio.models.tool_call_agui import ToolCallAGUI

_ALIASES = {
    "id": "id",
    "role": "role",
    "content": "content",
    "name": "name",
    "tool_calls": "toolCalls",
    "encrypted_content": "encryptedContent",
    "parts": "parts",
    "created_at": "createdAt",
}


def _alias_generator(name: str) -> str:
    return _ALIASES.get(name, name)


class AssistantMessageAGUI(BaseModel):
    """
    An official AG-UI assistant message or a TanStack ``UIMessage`` extension.  Standard AG-UI represents text through ``content`` and tool calls through ``toolCalls``. TanStack sends those fields alongside its authoritative ``parts`` array. A populated ``parts`` array takes precedence during conversion, preventing the redundant fields from duplicating history. Unknown vendor fields are intentionally ignored, matching the other AG-UI message variants; TanStack, for example, attaches its own ``metadata`` object.
    """

    id: str
    role: str
    content: Optional[str] = None
    name: Optional[str] = None
    tool_calls: Optional[List[ToolCallAGUI]] = None
    encrypted_content: Optional[str] = None
    parts: Optional[List[MessagePartAssistantMessageAGUI]] = None
    created_at: Optional[str] = None

    model_config = ConfigDict(
        strict=False,
        use_enum_values=True,
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
        alias_generator=_alias_generator,
        extra="allow",
    )

    def to_json(self) -> str:
        return self.model_dump_json(by_alias=True, exclude_unset=True)

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AssistantMessageAGUI from a JSON string"""
        return cls.from_dict(loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias."""
        return self.model_dump(
            by_alias=True,
            exclude_none=True,
            exclude_unset=True,
        )

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AssistantMessageAGUI from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj["toolCalls"] = (
            [ToolCallAGUI.from_dict(_item) for _item in obj["toolCalls"]]
            if obj.get("toolCalls") is not None
            else None
        )
        obj["parts"] = (
            [MessagePartAssistantMessageAGUI.from_dict(_item) for _item in obj["parts"]]
            if obj.get("parts") is not None
            else None
        )

        return cls.model_validate(obj)

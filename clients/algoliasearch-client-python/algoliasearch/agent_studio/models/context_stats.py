# coding: utf-8

"""
Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
"""

from __future__ import annotations

from json import loads
from sys import version_info
from typing import Any, Dict, Optional

from pydantic import BaseModel, ConfigDict

if version_info >= (3, 11):
    from typing import Self
else:
    from typing_extensions import Self


_ALIASES = {
    "messages_before": "messagesBefore",
    "messages_after": "messagesAfter",
    "tokens_before_estimate": "tokensBeforeEstimate",
    "tokens_after_estimate": "tokensAfterEstimate",
    "messages_dropped": "messagesDropped",
}


def _alias_generator(name: str) -> str:
    return _ALIASES.get(name, name)


class ContextStats(BaseModel):
    """
    Before/after accounting for a context operation.  ``tokens*Estimate`` fields are heuristic (tiktoken BPE over the serialized messages), not an authoritative provider count. ``messages_dropped`` is the net reduction in message count (``messages_before - messages_after``); for compaction the summarized tail is replaced by a single summary message, so this reflects the net change rather than a raw delete count.
    """

    messages_before: int
    """ Message count before the operation. """
    messages_after: int
    """ Message count after the operation. """
    tokens_before_estimate: int
    """ Estimated token count before the operation. """
    tokens_after_estimate: int
    """ Estimated token count after the operation. """
    messages_dropped: int
    """ Net reduction in message count (before - after). """

    model_config = ConfigDict(
        strict=False,
        use_enum_values=True,
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
        alias_generator=_alias_generator,
        extra="allow",
    )

    def to_json(self) -> str:
        return self.model_dump_json(by_alias=True, exclude_unset=True)

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ContextStats from a JSON string"""
        return cls.from_dict(loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias."""
        return self.model_dump(
            by_alias=True,
            exclude_none=True,
            exclude_unset=True,
        )

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ContextStats from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        return cls.model_validate(obj)

/** Agent Studio API Algolia's GenAI Toolkit API, building blocks for your dynamic LLM-enabled experiences. Build
  * generative AI experiences with your Algolia data. Combine large language models with Algolia’s Search API using
  * Retrieval Augmented Generation (RAG) techniques.
  *
  * The version of the OpenAPI document: 0.1.0
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech Do not edit the class manually.
  */
package algoliasearch.agentstudio

import org.json4s._

/** ToolConfig
  */
sealed trait ToolConfig

trait ToolConfigTrait extends ToolConfig

object ToolConfig {

  case class BooleanValue(value: Boolean) extends ToolConfig

  def apply(value: Boolean): ToolConfig = {
    ToolConfig.BooleanValue(value)
  }

}

object ToolConfigSerializer extends Serializer[ToolConfig] {
  override def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), ToolConfig] = {

    case (TypeInfo(clazz, _), json) if clazz == classOf[ToolConfig] =>
      json match {
        case value: JObject => Extraction.extract[McpToolConfig](value)
        case JBool(value)   => ToolConfig.BooleanValue(value)
        case _              => throw new MappingException("Can't convert " + json + " to ToolConfig")
      }
  }

  override def serialize(implicit format: Formats): PartialFunction[Any, JValue] = { case value: ToolConfig =>
    value match {
      case value: McpToolConfig           => Extraction.decompose(value)(format - this)
      case ToolConfig.BooleanValue(value) => JBool(value)
    }
  }
}

/** ragApi Algolia's GenAI Toolkit API, building blocks for your dynamic LLM-enabled experiences. Build generative AI
  * experiences with your Algolia data. Combine large language models with Algolia’s Search API using Retrieval
  * Augmented Generation (RAG) techniques.
  *
  * The version of the OpenAPI document: 0.1.0
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech Do not edit the class manually.
  */
package algoliasearch.agentstudio

import org.json4s._

/** AroundRadiusUnion
  */
sealed trait AroundRadiusUnion

object AroundRadiusUnion {

  case class IntValue(value: Int) extends AroundRadiusUnion

  case class StringValue(value: String) extends AroundRadiusUnion

  def apply(value: Int): AroundRadiusUnion = {
    AroundRadiusUnion.IntValue(value)
  }

  def apply(value: String): AroundRadiusUnion = {
    AroundRadiusUnion.StringValue(value)
  }

}

object AroundRadiusUnionSerializer extends Serializer[AroundRadiusUnion] {
  override def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), AroundRadiusUnion] = {

    case (TypeInfo(clazz, _), json) if clazz == classOf[AroundRadiusUnion] =>
      json match {
        case JInt(value)    => AroundRadiusUnion.IntValue(value.toInt)
        case JString(value) => AroundRadiusUnion.StringValue(value)
        case _              => throw new MappingException("Can't convert " + json + " to AroundRadiusUnion")
      }
  }

  override def serialize(implicit format: Formats): PartialFunction[Any, JValue] = { case value: AroundRadiusUnion =>
    value match {
      case AroundRadiusUnion.IntValue(value)    => JInt(value)
      case AroundRadiusUnion.StringValue(value) => JString(value)
    }
  }
}

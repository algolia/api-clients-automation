/** Agent Studio API The Agent Studio API lets you build and operate generative AI agents that use Algolia data, tools,
  * and your chosen LLM provider. Use it to create, configure, publish, and update agents, then generate chat
  * completions grounded in live data from your Algolia indices. You can manage LLM providers, Agent Studio tools,
  * conversations, feedback, secret keys, user data, caching, and application settings for your AI experiences. ##
  * Client libraries Use Algolia's API clients and libraries to reliably integrate Algolia's APIs with your apps. For
  * more information, see [Algolia's ecosystem](https://www.algolia.com/doc/libraries). ## Base URL Base URL for the
  * Agent Studio API: - `https://{APPLICATION_ID}.algolia.net/agent-studio` **All requests must use HTTPS.** ##
  * Authentication Add these headers to authenticate requests: - `x-algolia-application-id`. Your Algolia application
  * ID. - `x-algolia-api-key`. An API key with the necessary permissions to make the request. The required access
  * control list (ACL) to make a request is listed in each endpoint's reference. You can find your application ID and
  * API key in the [Algolia dashboard](https://dashboard.algolia.com/account/api-keys). ## Request format Request bodies
  * must be JSON objects. ## Response status and errors The Agent Studio API returns JSON responses. Since JSON doesn't
  * guarantee any specific ordering, don't rely on the order of attributes in the API response. Successful responses
  * return `2xx` statuses. Client errors return `4xx` statuses. Server errors return `5xx` statuses. Error responses
  * have a `message` property with more information. ## Version The current version of the Agent Studio API is version
  * 1, indicated by the `/1/` in each endpoint's URL.
  *
  * The version of the OpenAPI document: 0.1.0
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech Do not edit the class manually.
  */
package algoliasearch.agentstudio

import org.json4s._

/** ProviderInputNullable
  */
sealed trait ProviderInputNullable

trait ProviderInputNullableTrait extends ProviderInputNullable

object ProviderInputNullable {}

object ProviderInputNullableSerializer extends Serializer[ProviderInputNullable] {
  override def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), ProviderInputNullable] = {

    case (TypeInfo(clazz, _), json) if clazz == classOf[ProviderInputNullable] =>
      json match {
        case value: JObject
            if value.obj.exists(_._1 == "azureDeployment") && value.obj.exists(_._1 == "azureEndpoint") =>
          Extraction.extract[AzureOpenAIProviderInput](value)
        case value: JObject if value.obj.exists(_._1 == "baseUrl") && value.obj.exists(_._1 == "defaultModel") =>
          Extraction.extract[OpenAICompatibleProviderInput](value)
        case value: JObject => Extraction.extract[OpenAIProviderInput](value)
        case value: JObject => Extraction.extract[BaseProviderInput](value)
        case value: JObject => Extraction.extract[AnthropicProviderInput](value)
        case _              => throw new MappingException("Can't convert " + json + " to ProviderInputNullable")
      }
  }

  override def serialize(implicit format: Formats): PartialFunction[Any, JValue] = {
    case value: ProviderInputNullable =>
      value match {
        case value: AzureOpenAIProviderInput      => Extraction.decompose(value)(format - this)
        case value: OpenAICompatibleProviderInput => Extraction.decompose(value)(format - this)
        case value: OpenAIProviderInput           => Extraction.decompose(value)(format - this)
        case value: BaseProviderInput             => Extraction.decompose(value)(format - this)
        case value: AnthropicProviderInput        => Extraction.decompose(value)(format - this)
      }
  }
}

/** ragApi Algolia's GenAI Toolkit API, building blocks for your dynamic LLM-enabled experiences. Build generative AI
  * experiences with your Algolia data. Combine large language models with Algolia’s Search API using Retrieval
  * Augmented Generation (RAG) techniques.
  *
  * The version of the OpenAPI document: 0.1.0
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech Do not edit the class manually.
  */
package algoliasearch.agentstudio

import algoliasearch.agentstudio.MemoryType._

import org.json4s._

/** Universal storage model for all memory types (semantic, episodic). This is the ONLY model that touches storage
  * (Algolia). Domain models (SemanticMemory, EpisodicMemory) are used for LLM extraction and converted to MemoryRecord
  * before saving. See https://langchain-ai.github.io/langmem/concepts/conceptual_guide/#memory-types for memory type
  * definitions.
  *
  * @param text
  *   Self-contained, first-person memory for long-term recall.
  * @param rawExtract
  *   Verbatim conversation extract, not paraphrased.
  * @param keywords
  *   5-20 free-form keywords: entities, context, search terms (any words).
  * @param topics
  *   2-4 topics ONLY from this list: [complaints, entertainment, family, feedback, finance, food, goals, health,
  *   history, hobbies, learning, praise, preferences, schedule, shopping, technical, travel, work].
  * @param tags
  *   Arbitrary labels/themes for flexible categorization (e.g., 'Q1-goals', 'paris-trip', 'vip-customer').
  * @param recallTriggers
  *   3-5 natural phrases that should trigger this memory.
  * @param appId
  *   Application ID.
  * @param agentIDs
  *   Agent IDs with access: ['agent1'], ['*'] for all, ['*', '-agent1'] to exclude.
  * @param userID
  *   User ID.
  * @param createdAt
  *   Epoch seconds.
  * @param updatedAt
  *   Epoch seconds.
  */
case class MemoryRecord(
    memoryType: Option[MemoryType] = scala.None,
    episode: Option[Episode] = scala.None,
    text: String,
    rawExtract: String,
    keywords: Option[Seq[String]] = scala.None,
    topics: Option[Seq[String]] = scala.None,
    tags /* _tags */: Option[Seq[String]] = scala.None,
    recallTriggers: Option[Seq[String]] = scala.None,
    objectID: Option[String] = scala.None,
    appId: Option[String] = scala.None,
    agentIDs: Option[Seq[String]] = scala.None,
    userID: Option[String] = scala.None,
    createdAt: Option[Int] = scala.None,
    updatedAt: Option[Int] = scala.None
)

class MemoryRecordSerializer extends Serializer[MemoryRecord] {

  private val renamedFields = Map[String, String](
    "_tags" -> "tags"
  )
  override def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), MemoryRecord] = {
    case (TypeInfo(clazz, _), json) if clazz == classOf[MemoryRecord] =>
      json match {
        case jobject: JObject =>
          // Rename fields from JSON to Scala
          val renamedObject = JObject(
            jobject.obj.map { field =>
              renamedFields.get(field._1).map(JField(_, field._2)).getOrElse(field)
            }
          )
          val formats = format - this
          val mf = manifest[MemoryRecord]
          Extraction.extract[MemoryRecord](renamedObject)(formats, mf)

        case _ => throw new IllegalArgumentException(s"Can't deserialize $json as MemoryRecord")
      }
  }

  override def serialize(implicit format: Formats): PartialFunction[Any, JValue] = { case value: MemoryRecord =>
    val formats = format - this // remove current serializer from formats to avoid stackoverflow
    val baseObj = Extraction.decompose(value)(formats)
    baseObj transformField {
      case JField(name, value) if renamedFields.exists(_._2 == name) => (renamedFields.find(_._2 == name).get._1, value)
    }
  }
}

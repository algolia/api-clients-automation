/** A/B Testing API ## Base URLs Base URLs for the A/B testing API: - `https://analytics.us.algolia.com` -
  * `https://analytics.de.algolia.com` - `https://analytics.algolia.com` (routes requests to the closest of the above
  * servers, based on your geographical location) Use the URL that matches your [analytics
  * region](https://dashboard.algolia.com/account/infrastructure/analytics). **All requests must use HTTPS.** ##
  * Availability and authentication Access to the A/B testing API is available as part of the [Premium or Elevate
  * plans](https://www.algolia.com/pricing). Add these headers to authenticate requests: - `x-algolia-application-id`.
  * Your Algolia application ID. - `x-algolia-api-key`. An API key with the necessary permissions to make the request.
  * The required access control list (ACL) to make a request is listed in each endpoint's reference. You can find your
  * application ID and API key in the [Algolia dashboard](https://dashboard.algolia.com/account/api-keys). ## Rate
  * limits You can make up to **100 requests per minute per app** to the A/B testing API. The response includes headers
  * with information about the limits. ## Parameters Query parameters must be
  * [URL-encoded](https://developer.mozilla.org/en-US/docs/Glossary/Percent-encoding). Non-ASCII characters must be
  * UTF-8 encoded. Plus characters (`+`) are interpreted as spaces. ## Response status and errors The A/B testing API
  * returns JSON responses. Since JSON doesn't guarantee any specific ordering, don't rely on the order of attributes in
  * the API response. Successful responses return `2xx` statuses. Client errors return `4xx` statuses. Server errors
  * return `5xx` statuses. Error responses have a `message` property with more information. ## Version The current
  * version of the A/B Testing API is version 3, indicated by the `/3/` in each endpoint's URL.
  *
  * The version of the OpenAPI document: 3.0.0
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech Do not edit the class manually.
  */
package algoliasearch.abtestingv3

/** Settings captured for one A/B test variant.
  *
  * @param variantId
  *   One-based index of the variant within the A/B test. The control is variant 1.
  * @param applied
  *   Whether the variant's settings have been applied to the control index.
  * @param featuresSettings
  *   Feature-specific settings captured for the variant.
  * @param indexSettings
  *   Index settings captured for the variant.
  * @param applyTimestamp
  *   Date and time when the variant's settings were applied to the control index, in RFC 3339 format.
  * @param revertTimestamp
  *   Date and time when the applied settings were reverted on the control index, in RFC 3339 format.
  * @param declaredTimestamp
  *   Date and time when the variant's settings were captured, in RFC 3339 format.
  * @param expiredTimestamp
  *   Date and time when the captured settings expired, in RFC 3339 format.
  */
case class VariantSettings(
    variantId: Int,
    applied: Boolean,
    featuresSettings: Option[Seq[FeatureSettings]] = scala.None,
    indexSettings: Option[Any] = scala.None,
    applyTimestamp: Option[String] = scala.None,
    revertTimestamp: Option[String] = scala.None,
    declaredTimestamp: Option[String] = scala.None,
    expiredTimestamp: Option[String] = scala.None
)

/** Analytics API ## Base URLs Base URLs for the Analytics API: - `https://analytics.us.algolia.com` -
  * `https://analytics.de.algolia.com` - `https://analytics.algolia.com` (alias of `analytics.us.algolia.com`) Use the
  * URL that matches your [analytics region](https://dashboard.algolia.com/account/infrastructure/analytics). **All
  * requests must use HTTPS.** ## Availability and authentication Access to the Analytics API is available as part of
  * the [Premium or Elevate plans](https://www.algolia.com/pricing). Add these headers to authenticate requests: -
  * `x-algolia-application-id`. Your Algolia application ID. - `x-algolia-api-key`. An API key with the necessary
  * permissions to make the request. The required access control list (ACL) to make a request is listed in each
  * endpoint's reference. You can find your application ID and API key in the [Algolia
  * dashboard](https://dashboard.algolia.com/account/api-keys). ## Rate limits You can make up to **100 requests per
  * minute per app** to the Analytics API. The response includes headers with information about the limits. ##
  * Parameters Query parameters must be
  * [URL-encoded](https://developer.mozilla.org/en-US/docs/Glossary/Percent-encoding). Non-ASCII characters must be
  * UTF-8 encoded. Plus characters (`+`) are interpreted as spaces. ## Response status and errors The Analytics API
  * returns JSON responses. Since JSON doesn't guarantee any specific ordering, don't rely on the order of attributes in
  * the API response. - Successful responses return a `2xx` status - Client errors return a `4xx` status - Server errors
  * are indicated by a `5xx` status. Error responses have a `message` property with more information. ## Version The
  * current version of the Analytics API is version 2, indicated by the `/2/` in each endpoint's URL. A semantic
  * patterns framework is also available under `/3/patterns/_*` for building custom analytics queries. ## Query
  * aggregation Algolia accepts queries on each keystroke. To ensure you have relevant analytics data, however, the
  * series of keystrokes is aggregated to keep only the latest (final) user query. This is called \"prefix\"
  * aggregation. For more information, see [Query agggregation and
  * processing](https://www.algolia.com/doc/guides/search-analytics/concepts/query-aggregation). See the analytics
  * implementation overview for more information about query aggregation.
  *
  * The version of the OpenAPI document: 2.0.0
  *
  * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
  * https://openapi-generator.tech Do not edit the class manually.
  */
package algoliasearch.analytics

import org.json4s._

/** Literal value. Its JSON type depends on `kind` (array of strings for `indices`, string for dates, tags, and country,
  * number for numeric operands, and so on).
  */
sealed trait ParameterValue

object ParameterValue {

  case class StringValue(value: String) extends ParameterValue

  case class DoubleValue(value: Double) extends ParameterValue

  case class SeqOfString(value: Seq[String]) extends ParameterValue

  case class BooleanValue(value: Boolean) extends ParameterValue

  def apply(value: String): ParameterValue = {
    ParameterValue.StringValue(value)
  }

  def apply(value: Double): ParameterValue = {
    ParameterValue.DoubleValue(value)
  }

  def apply(value: Seq[String]): ParameterValue = {
    ParameterValue.SeqOfString(value)
  }

  def apply(value: Boolean): ParameterValue = {
    ParameterValue.BooleanValue(value)
  }

}

object ParameterValueSerializer extends Serializer[ParameterValue] {
  override def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), ParameterValue] = {

    case (TypeInfo(clazz, _), json) if clazz == classOf[ParameterValue] =>
      json match {
        case JString(value) => ParameterValue.StringValue(value)
        case JDouble(value) => ParameterValue.DoubleValue(value.toDouble)
        case value: JArray  => ParameterValue.apply(Extraction.extract[Seq[String]](value))
        case JBool(value)   => ParameterValue.BooleanValue(value)
        case _              => throw new MappingException("Can't convert " + json + " to ParameterValue")
      }
  }

  override def serialize(implicit format: Formats): PartialFunction[Any, JValue] = { case value: ParameterValue =>
    value match {
      case ParameterValue.StringValue(value)  => JString(value)
      case ParameterValue.DoubleValue(value)  => JDouble(value)
      case ParameterValue.SeqOfString(value)  => JArray(value.map(Extraction.decompose).toList)
      case ParameterValue.BooleanValue(value) => JBool(value)
    }
  }
}

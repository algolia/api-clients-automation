// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost
// - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.

package com.algolia.model.agentstudio;

import com.algolia.exceptions.AlgoliaRuntimeException;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.core.*;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.*;
import com.fasterxml.jackson.databind.annotation.*;
import java.io.IOException;
import java.util.List;
import java.util.logging.Logger;

/** FacetFiltersOutputUnion */
@JsonDeserialize(using = FacetFiltersOutputUnion.Deserializer.class)
public interface FacetFiltersOutputUnion {
  // FacetFiltersOutputUnion as List<FacetFiltersOutputUnion> wrapper.
  static FacetFiltersOutputUnion of(List<FacetFiltersOutputUnion> value) {
    return new ListOfFacetFiltersOutputUnionWrapper(value);
  }

  // FacetFiltersOutputUnion as String wrapper.
  static FacetFiltersOutputUnion of(String value) {
    return new StringWrapper(value);
  }

  // FacetFiltersOutputUnion as List<FacetFiltersOutputUnion> wrapper.
  @JsonSerialize(using = ListOfFacetFiltersOutputUnionWrapper.Serializer.class)
  class ListOfFacetFiltersOutputUnionWrapper implements FacetFiltersOutputUnion {

    private final List<FacetFiltersOutputUnion> value;

    ListOfFacetFiltersOutputUnionWrapper(List<FacetFiltersOutputUnion> value) {
      this.value = value;
    }

    public List<FacetFiltersOutputUnion> getValue() {
      return value;
    }

    static class Serializer extends JsonSerializer<ListOfFacetFiltersOutputUnionWrapper> {

      @Override
      public void serialize(ListOfFacetFiltersOutputUnionWrapper value, JsonGenerator gen, SerializerProvider provider) throws IOException {
        gen.writeObject(value.getValue());
      }
    }
  }

  // FacetFiltersOutputUnion as String wrapper.
  @JsonSerialize(using = StringWrapper.Serializer.class)
  class StringWrapper implements FacetFiltersOutputUnion {

    private final String value;

    StringWrapper(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    static class Serializer extends JsonSerializer<StringWrapper> {

      @Override
      public void serialize(StringWrapper value, JsonGenerator gen, SerializerProvider provider) throws IOException {
        gen.writeObject(value.getValue());
      }
    }
  }

  class Deserializer extends JsonDeserializer<FacetFiltersOutputUnion> {

    private static final Logger LOGGER = Logger.getLogger(Deserializer.class.getName());

    @Override
    public FacetFiltersOutputUnion deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException {
      JsonNode tree = jp.readValueAsTree();
      // deserialize List<FacetFiltersOutputUnion>
      if (tree.isArray()) {
        try (JsonParser parser = tree.traverse(jp.getCodec())) {
          List<FacetFiltersOutputUnion> value = parser.readValueAs(new TypeReference<List<FacetFiltersOutputUnion>>() {});
          return new FacetFiltersOutputUnion.ListOfFacetFiltersOutputUnionWrapper(value);
        } catch (Exception e) {
          // deserialization failed, continue
          LOGGER.finest(
            "Failed to deserialize oneOf List<FacetFiltersOutputUnion> (error: " +
              e.getMessage() +
              ") (type: List<FacetFiltersOutputUnion>)"
          );
        }
      }
      // deserialize String
      if (tree.isTextual()) {
        try (JsonParser parser = tree.traverse(jp.getCodec())) {
          String value = parser.readValueAs(String.class);
          return new FacetFiltersOutputUnion.StringWrapper(value);
        } catch (Exception e) {
          // deserialization failed, continue
          LOGGER.finest("Failed to deserialize oneOf String (error: " + e.getMessage() + ") (type: String)");
        }
      }
      throw new AlgoliaRuntimeException(String.format("Failed to deserialize json element: %s", tree));
    }

    /** Handle deserialization of the 'null' value. */
    @Override
    public FacetFiltersOutputUnion getNullValue(DeserializationContext ctxt) throws JsonMappingException {
      return null;
    }
  }
}

PaginatedSecretKeysResponse:
  properties:
    data:
      items:
        $ref: SecretKeyResponse.yml#/SecretKeyResponse
      type: array
      title: data
    pagination:
      $ref: PaginationMetadata.yml#/PaginationMetadata
  type: object
  required:
  - data
  - pagination
  title: paginatedSecretKeysResponse

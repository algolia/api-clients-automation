FacetsParam:
  description: A facets parameter that is always hidden from the LLM.
  properties:
    exposed:
      const: false
      default: false
      title: exposed
      type: boolean
    default:
      oneOf:
      - items:
          type: string
        type: array
      - type: 'null'
      default: null
      title: default
  title: facetsParam
  type: object

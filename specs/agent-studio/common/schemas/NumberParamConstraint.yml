NumberParamConstraint:
  description: Constraints for a number parameter.
  properties:
    min:
      oneOf:
      - type: integer
      - type: 'null'
      default: null
      title: min
    max:
      oneOf:
      - type: integer
      - type: 'null'
      default: null
      title: max
  title: numberParamConstraint
  type: object

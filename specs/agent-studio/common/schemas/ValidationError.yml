ValidationError:
  properties:
    loc:
      items:
        $ref: LocationItemUnion.yml#/LocationItemUnion
      type: array
      title: location
    msg:
      type: string
      title: message
    type:
      type: string
      title: errorType
    input:
      title: input
    ctx:
      type: object
      title: context
  type: object
  required:
  - loc
  - msg
  - type
  title: validationError

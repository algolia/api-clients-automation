SecretKeyPatch:
  properties:
    name:
      type: string
      maxLength: 128
      minLength: 1
      title: name
      description: The new name of the secret key.
    agentIds:
      items:
        type: string
      type: array
      title: agentids
      description: Updated list of agent IDs this secret key is associated with.
  type: object
  title: secretKeyPatch
  description: Secret key patch payload.

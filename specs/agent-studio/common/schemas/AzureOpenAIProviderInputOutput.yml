AzureOpenAIProviderInput-Output:
  properties:
    apiKey:
      type: string
      title: apikey
    azureEndpoint:
      type: string
      title: azureendpoint
    azureDeployment:
      type: string
      minLength: 1
      title: azuredeployment
      description: Azure model deployment name is required.
    apiVersion:
      oneOf:
        - type: string
        - type: 'null'
      title: apiversion
      default: 2024-12-01-preview
  type: object
  required:
    - apiKey
    - azureEndpoint
    - azureDeployment
  title: azureOpenAIProviderInput
  description: Azure OpenAI-specific provider input.

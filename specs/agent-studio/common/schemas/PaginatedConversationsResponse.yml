PaginatedConversationsResponse:
  properties:
    data:
      items:
        $ref: ConversationBaseResponse.yml#/ConversationBaseResponse
      type: array
      title: data
    pagination:
      $ref: PaginationMetadata.yml#/PaginationMetadata
    analyticsDegraded:
      type: boolean
      title: analyticsdegraded
      default: false
  type: object
  required:
  - data
  - pagination
  title: paginatedConversationsResponse

PaginatedMemoriesResponse:
  properties:
    data:
      items:
        $ref: 'MemoryRecord.yml#/MemoryRecord'
      type: array
      title: data
    pagination:
      $ref: 'PaginationMetadata.yml#/PaginationMetadata'
  type: object
  required:
    - data
    - pagination
  title: paginatedMemoriesResponse
  description: Response model for paginated memories.

QueryExtractionResponse:
  properties:
    queries:
      items:
        type: string
      type: array
      title: queries
      description: Extracted search queries (1-3 optimized queries).
    messageCount:
      type: integer
      title: messagecount
      description: Number of messages processed.
    toolCount:
      type: integer
      title: toolcount
      description: Number of tool results extracted for keyword enrichment.
  type: object
  required:
    - queries
    - messageCount
    - toolCount
  title: queryExtractionResponse
  description: Response from query extraction (1-3 optimized queries).

TextParam:
  description: A text search parameter with exposure control.
  properties:
    exposed:
      title: exposed
      type: boolean
    default:
      oneOf:
      - type: string
      - type: 'null'
      default: null
      title: default
  required:
  - exposed
  title: textParam
  type: object

ToolCallPart:
  properties:
    type:
      type: string
      const: tool-call
      title: type
      default: tool-call
    toolCallId:
      type: string
      title: toolcallid
    toolName:
      type: string
      title: toolname
    args:
      title: args
    requiresApproval:
      type: boolean
      title: requiresapproval
    providerOptions:
      type: object
      additionalProperties: true
      title: provideroptions
  type: object
  required:
    - type
    - toolCallId
    - toolName
    - args
  title: toolCallPart

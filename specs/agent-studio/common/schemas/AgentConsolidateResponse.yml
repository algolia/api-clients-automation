AgentConsolidateResponse:
  properties:
    savedMemories:
      items:
        $ref: 'MemoryRecord.yml#/MemoryRecord'
      type: array
      title: savedmemories
      description: Memories saved/updated.
    deletedIds:
      items:
        type: string
      type: array
      title: deletedids
      description: Memory object IDs deleted during consolidation.
    saveTaskId:
      oneOf:
        - type: string
        - type: 'null'
      title: savetaskid
      description: Algolia task ID for save batch.
    deleteTaskIds:
      items:
        type: string
      type: array
      title: deletetaskids
      description: Algolia task IDs for deletes.
    message:
      type: string
      title: message
      description: Summary message of consolidation results.
  type: object
  required:
    - message
  title: agentConsolidateResponse
  description: Response model for agent consolidate endpoint.

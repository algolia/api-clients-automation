description: Success
content:
  application/json:
    schema:
      type: object
      title: SucessResponse
      additionalProperties: false
      required:
        - status
        - message
      properties:
        status:
          type: number
          example: 200
        message:
          type: string
          example: Success

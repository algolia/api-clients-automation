title: getTopFiltersNoResultsResponse
type: object
additionalProperties: false
required:
  - values
properties:
  values:
    type: array
    description: A list of filters without results.
    items:
      type: object
      additionalProperties: false
      required:
        - values
        - count
      properties:
        count:
          $ref: '../parameters.yml#/count'
        values:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - attribute
              - operator
              - value
            properties:
              attribute:
                $ref: '../parameters.yml#/attribute'
              operator:
                $ref: '../parameters.yml#/operator'
              value:
                $ref: '../parameters.yml#/value'

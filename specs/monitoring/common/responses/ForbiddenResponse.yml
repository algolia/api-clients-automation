description: Forbidden
content:
  application/json:
    schema:
      title: forbidden
      type: object
      additionalProperties: false
      properties:
        reason:
          type: string
    examples:
      Forbidden:
        value:
          reason: invalid credentials

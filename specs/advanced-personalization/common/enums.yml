errorCodes:
  title: errorCodes
  type: string
  enum:
    - blocked
    - deleted
    - no_perso_access
    - all_attr_value_filtered
    - missing_index
    - no_events_last_30_days
    - pending_pipelines
    - invalid_config
    - all_facets_invalid

status:
  title: status
  type: string
  enum: [pending, active, paused]

sourceType:
  title: type
  type: string
  enum: [insights]

strategy:
  type: string
  enum:
    - session
    - historical
    - hybrid
  example: 'session'

userType:
  title: userType
  type: string
  enum: [basic, predictive, realtime]

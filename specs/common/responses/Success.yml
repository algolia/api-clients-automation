description: Success
content:
  application/json:
    schema:
      title: successResponse
      type: object
      additionalProperties: false
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 200
          description: Status code.
        message:
          type: string
          example: Success
          description: Response message.

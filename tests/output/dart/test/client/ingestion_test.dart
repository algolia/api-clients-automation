// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
import 'package:algolia_client_ingestion/algolia_client_ingestion.dart';
import 'package:algolia_test/algolia_test.dart';
import 'package:test/test.dart';
import 'package:test_api/hooks.dart';
import 'dart:io' as io;

void main() {
  test('can handle HTML error', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
        appId: "test-app-id",
        apiKey: "test-api-key",
        region: 'us',
        options: ClientOptions(hosts: [
          Host.create(
              url:
                  '${io.Platform.environment['CI'] == 'true' ? 'localhost' : 'host.docker.internal'}:6676',
              scheme: 'http'),
        ]));

    await expectError(
      '429',
      () async {
        try {
          final res = await client.customGet(
            path: "1/html-error",
          );
        } on InterceptionException catch (_) {
          // Ignore InterceptionException
        }
      },
    );
  });

  test('calls api with default read timeouts', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
      appId: 'appId',
      apiKey: 'apiKey',
      region: 'us',
      options: ClientOptions(requester: requester),
    );
    requester.setOnRequest((request) {
      expect(25000, request.timeout.inMilliseconds);
    });
    try {
      final res = await client.customGet(
        path: "1/test",
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('calls api with default write timeouts', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
      appId: 'appId',
      apiKey: 'apiKey',
      region: 'us',
      options: ClientOptions(requester: requester),
    );
    requester.setOnRequest((request) {
      expect(25000, request.timeout.inMilliseconds);
    });
    try {
      final res = await client.customPost(
        path: "1/test",
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('can leave call opened for a long time', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
        appId: "test-app-id",
        apiKey: "test-api-key",
        region: 'us',
        options: ClientOptions(hosts: [
          Host.create(
              url:
                  '${io.Platform.environment['CI'] == 'true' ? 'localhost' : 'host.docker.internal'}:6676',
              scheme: 'http'),
        ]));

    requester.setOnRequest((request) {});
    try {
      final res = await client.customGet(
        path: "1/long-wait",
      );
      expectBody(res, """{"message":"OK"}""");
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('endpoint level timeout', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
      appId: 'appId',
      apiKey: 'apiKey',
      region: 'us',
      options: ClientOptions(requester: requester),
    );
    requester.setOnRequest((request) {
      expect(180000, request.timeout.inMilliseconds);
    });
    try {
      final res = await client.validateSourceBeforeUpdate(
        sourceID: "6c02aeb1-775e-418e-870b-1faccd4b2c0f",
        sourceUpdate: SourceUpdate(
          name: "newName",
        ),
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('can override endpoint level timeout', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
      appId: 'appId',
      apiKey: 'apiKey',
      region: 'us',
      options: ClientOptions(requester: requester),
    );
    requester.setOnRequest((request) {
      expect(3456, request.timeout.inMilliseconds);
    });
    try {
      final res = await client.validateSourceBeforeUpdate(
        sourceID: "6c02aeb1-775e-418e-870b-1faccd4b2c0f",
        sourceUpdate: SourceUpdate(
          name: "newName",
        ),
        requestOptions: RequestOptions(
          writeTimeout: Duration(milliseconds: 3456),
        ),
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('calls api with correct user agent', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
      appId: 'appId',
      apiKey: 'apiKey',
      region: 'us',
      options: ClientOptions(requester: requester),
    );
    requester.setOnRequest((request) {
      TestHandle.current.markSkipped('User agent added using an interceptor');
    });
    try {
      final res = await client.customPost(
        path: "1/test",
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('the user agent contains the latest version', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
      appId: 'appId',
      apiKey: 'apiKey',
      region: 'us',
      options: ClientOptions(requester: requester),
    );
    requester.setOnRequest((request) {
      TestHandle.current.markSkipped('User agent added using an interceptor');
    });
    try {
      final res = await client.customPost(
        path: "1/test",
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('uses the correct region', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
        appId: "my-app-id",
        apiKey: "my-api-key",
        region: 'us',
        options: ClientOptions(requester: requester));

    requester.setOnRequest((request) {
      expect(request.host.url, "data.us.algolia.com");
    });
    try {
      final res = await client.getSource(
        sourceID: "6c02aeb1-775e-418e-870b-1faccd4b2c0f",
      );
    } on InterceptionException catch (_) {
      // Ignore InterceptionException
    }
  });

  test('throws when incorrect region is given', () async {
    final requester = RequestInterceptor();
    await expectError(
      '`region` is required and must be one of the following: eu, us',
      () async {
        final client = IngestionClient(
            appId: "my-app-id",
            apiKey: "my-api-key",
            region: 'not_a_region',
            options: ClientOptions(requester: requester));
      },
    );
  });

  test('switch API key', () async {
    final requester = RequestInterceptor();
    final client = IngestionClient(
        appId: "test-app-id",
        apiKey: "test-api-key",
        region: 'us',
        options: ClientOptions(hosts: [
          Host.create(
              url:
                  '${io.Platform.environment['CI'] == 'true' ? 'localhost' : 'host.docker.internal'}:6683',
              scheme: 'http'),
        ]));

    {
      requester.setOnRequest((request) {});
      try {
        final res = await client.customGet(
          path: "check-api-key/1",
        );
        expectBody(res, """{"headerAPIKeyValue":"test-api-key"}""");
      } on InterceptionException catch (_) {
        // Ignore InterceptionException
      }
    }
    {
      try {
        client.setClientApiKey(
          apiKey: "updated-api-key",
        );
      } on InterceptionException catch (_) {
        // Ignore InterceptionException
      }
    }
    {
      requester.setOnRequest((request) {});
      try {
        final res = await client.customGet(
          path: "check-api-key/2",
        );
        expectBody(res, """{"headerAPIKeyValue":"updated-api-key"}""");
      } on InterceptionException catch (_) {
        // Ignore InterceptionException
      }
    }
  });
}

// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on
// https://github.com/algolia/api-clients-automation. DO NOT EDIT.
import XCTest

import Utils

@testable import AlgoliaAgentStudio
@testable import AlgoliaCore

final class AgentStudioClientRequestsTests: XCTestCase {
    static let APPLICATION_ID = "my_application_id"
    static let API_KEY = "my_api_key"

    /// bulkCreateAllowedDomains
    func testBulkCreateAllowedDomainsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.bulkCreateAllowedDomainsWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            allowedDomainBulkInsert: AllowedDomainBulkInsert(domains: ["https://app.example.com", "*.example.org"])
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"domains\":[\"https://app.example.com\",\"*.example.org\"]}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains/bulk"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// bulkDeleteAllowedDomains
    func testBulkDeleteAllowedDomainsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.bulkDeleteAllowedDomainsWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            allowedDomainBulkDelete: AllowedDomainBulkDelete(domainIds: [
                "a1b2c3d4-5678-90ab-cdef-123456789abc",
                "b2c3d4e5-6789-01ab-cdef-234567890abc",
            ])
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"domainIds\":[\"a1b2c3d4-5678-90ab-cdef-123456789abc\",\"b2c3d4e5-6789-01ab-cdef-234567890abc\"]}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains/bulk"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createAgent with minimal parameters
    func testCreateAgentTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createAgentWithHTTPInfo(agentConfigCreate: AgentConfigCreate(
            name: "test-agent",
            instructions: "You are a helpful assistant."
        ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"test-agent\",\"instructions\":\"You are a helpful assistant.\"}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createAgent with all parameters
    func testCreateAgentTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createAgentWithHTTPInfo(agentConfigCreate: AgentConfigCreate(
            name: "test-agent",
            description: "A test agent for CTS",
            providerId: "c2905529-b933-4b69-87ec-75f9829d5f59",
            model: "gpt-4",
            instructions: "You are a helpful assistant.",
            config: ["sendUsage": true, "sendReasoning": true, "temperature": 0.7, "max_tokens": 1500],
            tools: [ToolConfigInput.algoliaDisplayResultsToolConfig(AlgoliaDisplayResultsToolConfig(type: "start"))]
        ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"test-agent\",\"description\":\"A test agent for CTS\",\"providerId\":\"c2905529-b933-4b69-87ec-75f9829d5f59\",\"model\":\"gpt-4\",\"instructions\":\"You are a helpful assistant.\",\"config\":{\"sendUsage\":true,\"sendReasoning\":true,\"temperature\":0.7,\"max_tokens\":1500},\"tools\":[{\"type\":\"start\"}]}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createAgentAllowedDomain
    func testCreateAgentAllowedDomainTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createAgentAllowedDomainWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            allowedDomainCreate: AllowedDomainCreate(domain: "https://app.example.com")
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"domain\":\"https://app.example.com\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createAgentCompletion with v4 messages
    func testCreateAgentCompletionTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createAgentCompletionWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            compatibilityMode: CompatibilityMode.aiSdk4,
            agentCompletionRequest: AgentCompletionRequest(messages: MessagesUnion
                .arrayOfMessageV4([MessageV4.userMessageV4(UserMessageV4(
                    role: "user",
                    content: "Hello, how are you?"
                ))]))
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"messages\":[{\"role\":\"user\",\"content\":\"Hello, how are you?\"}]}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/completions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"compatibilityMode\":\"ai-sdk-4\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// createFeedback with required parameters
    func testCreateFeedbackTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createFeedbackWithHTTPInfo(feedbackCreationRequest: FeedbackCreationRequest(
            messageId: "msg-abc123",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            vote: XCTUnwrap(VoteEnum(rawValue: 1))
        ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"messageId\":\"msg-abc123\",\"agentId\":\"76710f1b-8231-42e5-b0d1-f43aac618e15\",\"vote\":1}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/feedback")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createFeedback with all parameters
    func testCreateFeedbackTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createFeedbackWithHTTPInfo(feedbackCreationRequest: FeedbackCreationRequest(
            messageId: "msg-abc123",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            vote: XCTUnwrap(VoteEnum(rawValue: 0)),
            tags: ["unhelpful", "off-topic"],
            notes: "The response did not address my question."
        ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"messageId\":\"msg-abc123\",\"agentId\":\"76710f1b-8231-42e5-b0d1-f43aac618e15\",\"vote\":0,\"tags\":[\"unhelpful\",\"off-topic\"],\"notes\":\"The response did not address my question.\"}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/feedback")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createProvider with OpenAI
    func testCreateProviderTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .createProviderWithHTTPInfo(providerAuthenticationCreate: ProviderAuthenticationCreate(
                name: "My OpenAI Provider",
                providerName: ProviderName.openai,
                input: ProviderInput.openAIProviderInput(OpenAIProviderInput(apiKey: "sk-test-key-1234"))
            ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"My OpenAI Provider\",\"providerName\":\"openai\",\"input\":{\"apiKey\":\"sk-test-key-1234\"}}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createProvider with Azure OpenAI
    func testCreateProviderTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .createProviderWithHTTPInfo(providerAuthenticationCreate: ProviderAuthenticationCreate(
                name: "My Azure Provider",
                providerName: ProviderName.azureOpenai,
                input: ProviderInput.azureOpenAIProviderInput(AzureOpenAIProviderInput(
                    apiKey: "az-test-key-5678",
                    azureEndpoint: "https://my-resource.openai.azure.com",
                    azureDeployment: "gpt-4o"
                ))
            ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"My Azure Provider\",\"providerName\":\"azure_openai\",\"input\":{\"apiKey\":\"az-test-key-5678\",\"azureEndpoint\":\"https://my-resource.openai.azure.com\",\"azureDeployment\":\"gpt-4o\"}}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createSecretKey with minimal parameters
    func testCreateSecretKeyTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .createSecretKeyWithHTTPInfo(secretKeyCreate: SecretKeyCreate(name: "my-secret-key"))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"my-secret-key\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// createSecretKey with agent IDs
    func testCreateSecretKeyTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.createSecretKeyWithHTTPInfo(secretKeyCreate: SecretKeyCreate(
            name: "scoped-key",
            agentIds: ["76710f1b-8231-42e5-b0d1-f43aac618e15"]
        ))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"scoped-key\",\"agentIds\":[\"76710f1b-8231-42e5-b0d1-f43aac618e15\"]}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// allow del method for a custom path with minimal parameters
    func testCustomDeleteTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customDeleteWithHTTPInfo(path: "test/minimal")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/minimal")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// allow del method for a custom path with all parameters
    func testCustomDeleteTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customDeleteWithHTTPInfo(
            path: "test/all",
            parameters: ["query": AnyCodable("parameters")]
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/all")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// allow get method for a custom path with minimal parameters
    func testCustomGetTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customGetWithHTTPInfo(path: "test/minimal")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/minimal")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// allow get method for a custom path with all parameters
    func testCustomGetTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customGetWithHTTPInfo(
            path: "test/all",
            parameters: ["query": AnyCodable("parameters with space")]
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/all")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters%20with%20space\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions should be escaped too
    func testCustomGetTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customGetWithHTTPInfo(
            path: "test/all",
            parameters: ["query": AnyCodable("to be overridden")],
            requestOptions: RequestOptions(
                headers: ["x-header-1": "spaces are left alone"],
                queryParameters: ["query": "parameters with space", "and an array": ["array", "with spaces"]]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/all")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap(
            "{\"query\":\"parameters%20with%20space\",\"and%20an%20array\":\"array%2Cwith%20spaces\"}"
                .data(using: .utf8)
        )
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)

        let expectedHeaders = try XCTUnwrap("{\"x-header-1\":\"spaces are left alone\"}".data(using: .utf8))
        let expectedHeadersMap = try CodableHelper.jsonDecoder.decode([String: String?].self, from: expectedHeaders)

        let echoResponseHeaders = try XCTUnwrap(echoResponse.headers)
        for header in expectedHeadersMap {
            XCTAssertEqual(echoResponseHeaders[header.key.capitalized], header.value)
        }
    }

    /// allow post method for a custom path with minimal parameters
    func testCustomPostTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(path: "test/minimal")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/minimal")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// allow post method for a custom path with all parameters
    func testCustomPostTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/all",
            parameters: ["query": AnyCodable("parameters")],
            body: ["body": "parameters"]
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"body\":\"parameters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/all")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions can override default query parameters
    func testCustomPostTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["query": "myQueryParameter"]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"myQueryParameter\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions merges query parameters with default ones
    func testCustomPostTest3() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["query2": "myQueryParameter"]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\",\"query2\":\"myQueryParameter\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions can override default headers
    func testCustomPostTest4() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                headers: ["x-algolia-api-key": "ALGOLIA_API_KEY"]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)

        let expectedHeaders = try XCTUnwrap("{\"x-algolia-api-key\":\"ALGOLIA_API_KEY\"}".data(using: .utf8))
        let expectedHeadersMap = try CodableHelper.jsonDecoder.decode([String: String?].self, from: expectedHeaders)

        let echoResponseHeaders = try XCTUnwrap(echoResponse.headers)
        for header in expectedHeadersMap {
            XCTAssertEqual(echoResponseHeaders[header.key.capitalized], header.value)
        }
    }

    /// requestOptions merges headers with default ones
    func testCustomPostTest5() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                headers: ["x-algolia-api-key": "ALGOLIA_API_KEY"]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)

        let expectedHeaders = try XCTUnwrap("{\"x-algolia-api-key\":\"ALGOLIA_API_KEY\"}".data(using: .utf8))
        let expectedHeadersMap = try CodableHelper.jsonDecoder.decode([String: String?].self, from: expectedHeaders)

        let echoResponseHeaders = try XCTUnwrap(echoResponse.headers)
        for header in expectedHeadersMap {
            XCTAssertEqual(echoResponseHeaders[header.key.capitalized], header.value)
        }
    }

    /// requestOptions queryParameters accepts booleans
    func testCustomPostTest6() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["isItWorking": true]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\",\"isItWorking\":\"true\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions queryParameters accepts integers
    func testCustomPostTest7() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["myParam": 2]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\",\"myParam\":\"2\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions queryParameters accepts list of string
    func testCustomPostTest8() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["myParam": ["b and c", "d"]]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\",\"myParam\":\"b%20and%20c%2Cd\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions queryParameters accepts list of booleans
    func testCustomPostTest9() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["myParam": [true, true, false]]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\",\"myParam\":\"true%2Ctrue%2Cfalse\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// requestOptions queryParameters accepts list of integers
    func testCustomPostTest10() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPostWithHTTPInfo(
            path: "test/requestOptions",
            parameters: ["query": AnyCodable("parameters")],
            body: ["facet": "filters"],
            requestOptions: RequestOptions(
                queryParameters: ["myParam": [1, 2]]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"facet\":\"filters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/requestOptions")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\",\"myParam\":\"1%2C2\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// allow put method for a custom path with minimal parameters
    func testCustomPutTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPutWithHTTPInfo(path: "test/minimal")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/minimal")
        XCTAssertEqual(echoResponse.method, HTTPMethod.put)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// allow put method for a custom path with all parameters
    func testCustomPutTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.customPutWithHTTPInfo(
            path: "test/all",
            parameters: ["query": AnyCodable("parameters")],
            body: ["body": "parameters"]
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"body\":\"parameters\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/test/all")
        XCTAssertEqual(echoResponse.method, HTTPMethod.put)

        let expectedQueryParameters = try XCTUnwrap("{\"query\":\"parameters\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// deleteAgent
    func testDeleteAgentTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteAgentWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// deleteAgentConversations without filters
    func testDeleteAgentConversationsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .deleteAgentConversationsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// deleteAgentConversations with date filters
    func testDeleteAgentConversationsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteAgentConversationsWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            startDate: "2024-01-01",
            endDate: "2024-06-30"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        let expectedQueryParameters = try XCTUnwrap("{\"startDate\":\"2024-01-01\",\"endDate\":\"2024-06-30\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// deleteAllowedDomain
    func testDeleteAllowedDomainTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteAllowedDomainWithHTTPInfo(
            domainId: "a1b2c3d4-5678-90ab-cdef-123456789abc",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains/a1b2c3d4-5678-90ab-cdef-123456789abc"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// deleteConversation
    func testDeleteConversationTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteConversationWithHTTPInfo(
            conversationId: "test-conversation-id",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations/test-conversation-id"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// deleteProvider
    func testDeleteProviderTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteProviderWithHTTPInfo(providerId: "c2905529-b933-4b69-87ec-75f9829d5f59")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// deleteSecretKey
    func testDeleteSecretKeyTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteSecretKeyWithHTTPInfo(secretKeyId: "a1b2c3d4-5678-90ab-cdef-123456789abc")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys/a1b2c3d4-5678-90ab-cdef-123456789abc")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// deleteUserData
    func testDeleteUserDataTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.deleteUserDataWithHTTPInfo(userToken: "test-user-token")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/user-data/test-user-token")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// exportConversations without filters
    func testExportConversationsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.exportConversationsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations/export"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// exportConversations with date filters
    func testExportConversationsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.exportConversationsWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            startDate: "2024-01-01",
            endDate: "2024-12-31"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations/export"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"startDate\":\"2024-01-01\",\"endDate\":\"2024-12-31\"}"
            .data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// e2e export conversations
    func testExportConversationsTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.exportConversationsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations/export"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getAgent
    func testGetAgentTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getAgentWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e get agent
    func testGetAgentTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getAgentWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getAllowedDomain
    func testGetAllowedDomainTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getAllowedDomainWithHTTPInfo(
            domainId: "a1b2c3d4-5678-90ab-cdef-123456789abc",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains/a1b2c3d4-5678-90ab-cdef-123456789abc"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e get allowed domain
    func testGetAllowedDomainTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getAllowedDomainWithHTTPInfo(
            domainId: "6e8a0441-9a41-477c-a1d6-679a461e0990",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains/6e8a0441-9a41-477c-a1d6-679a461e0990"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getConfiguration
    func testGetConfigurationTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getConfigurationWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/configuration")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e getConfiguration
    func testGetConfigurationTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getConfigurationWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/configuration")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getConversation
    func testGetConversationTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getConversationWithHTTPInfo(
            conversationId: "test-conversation-id",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations/test-conversation-id"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e get conversation
    func testGetConversationTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getConversationWithHTTPInfo(
            conversationId: "alg_cnv_miss_yqcZtaOSPTF8bJsJ",
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(
            echoResponse.path,
            "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations/alg_cnv_miss_yqcZtaOSPTF8bJsJ"
        )
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getProvider
    func testGetProviderTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getProviderWithHTTPInfo(providerId: "c2905529-b933-4b69-87ec-75f9829d5f59")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e get provider
    func testGetProviderTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getProviderWithHTTPInfo(providerId: "c2905529-b933-4b69-87ec-75f9829d5f59")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getSecretKey
    func testGetSecretKeyTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getSecretKeyWithHTTPInfo(secretKeyId: "a1b2c3d4-5678-90ab-cdef-123456789abc")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys/a1b2c3d4-5678-90ab-cdef-123456789abc")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e get secret key
    func testGetSecretKeyTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getSecretKeyWithHTTPInfo(secretKeyId: "c110e615-7d5a-4103-865f-c5062c0d6f4d")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys/c110e615-7d5a-4103-865f-c5062c0d6f4d")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// getUserData
    func testGetUserDataTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.getUserDataWithHTTPInfo(userToken: "test-user-token")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/user-data/test-user-token")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// invalidateAgentCache without before
    func testInvalidateAgentCacheTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .invalidateAgentCacheWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/cache")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// invalidateAgentCache with before date
    func testInvalidateAgentCacheTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.invalidateAgentCacheWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            before: "2024-12-01"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/cache")
        XCTAssertEqual(echoResponse.method, HTTPMethod.delete)

        let expectedQueryParameters = try XCTUnwrap("{\"before\":\"2024-12-01\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// listAgentAllowed-Domains
    func testListAgentAllowedDomainsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .listAgentAllowedDomainsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e list agent allowed domains
    func testListAgentAllowedDomainsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .listAgentAllowedDomainsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/allowed-domains")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// listAgentConversations with minimal parameters
    func testListAgentConversationsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .listAgentConversationsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// listAgentConversations with all parameters
    func testListAgentConversationsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listAgentConversationsWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            startDate: "2024-01-01",
            endDate: "2024-12-31",
            includeFeedback: true,
            feedbackVote: 1,
            page: 2,
            limit: 10,
            xAlgoliaSecureUserToken: nil
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap(
            "{\"startDate\":\"2024-01-01\",\"endDate\":\"2024-12-31\",\"includeFeedback\":\"true\",\"feedbackVote\":\"1\",\"page\":\"2\",\"limit\":\"10\"}"
                .data(using: .utf8)
        )
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// e2e list agent conversations
    func testListAgentConversationsTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .listAgentConversationsWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/conversations")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// list agents with no params
    func testListAgentsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listAgentsWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// list agents with all parameters
    func testListAgentsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listAgentsWithHTTPInfo(
            page: 2,
            limit: 5,
            providerId: "c2905529-b933-4b69-87ec-75f9829d5f59"
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap(
            "{\"page\":\"2\",\"limit\":\"5\",\"providerId\":\"c2905529-b933-4b69-87ec-75f9829d5f59\"}"
                .data(using: .utf8)
        )
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// e2e list agents
    func testListAgentsTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listAgentsWithHTTPInfo(page: 1, limit: 2, providerId: nil)
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"page\":\"1\",\"limit\":\"2\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// listModels
    func testListModelsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listModelsWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/models")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e list models
    func testListModelsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listModelsWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/models")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// listProviderModels
    func testListProviderModelsTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .listProviderModelsWithHTTPInfo(providerId: "c2905529-b933-4b69-87ec-75f9829d5f59")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59/models")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// e2e list provider models
    func testListProviderModelsTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .listProviderModelsWithHTTPInfo(providerId: "c2905529-b933-4b69-87ec-75f9829d5f59")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59/models")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// listProviders with no params
    func testListProvidersTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listProvidersWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// listProviders with pagination
    func testListProvidersTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listProvidersWithHTTPInfo(page: 2, limit: 5)
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"page\":\"2\",\"limit\":\"5\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// e2e list providers
    func testListProvidersTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listProvidersWithHTTPInfo(page: 1, limit: 2)
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"page\":\"1\",\"limit\":\"2\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// listSecretKeys with no params
    func testListSecretKeysTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listSecretKeysWithHTTPInfo()
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// listSecretKeys with pagination
    func testListSecretKeysTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listSecretKeysWithHTTPInfo(page: 2, limit: 5)
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"page\":\"2\",\"limit\":\"5\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// e2e list secret keys
    func testListSecretKeysTest2() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.listSecretKeysWithHTTPInfo(page: 1, limit: 10)
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        XCTAssertNil(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys")
        XCTAssertEqual(echoResponse.method, HTTPMethod.get)

        let expectedQueryParameters = try XCTUnwrap("{\"page\":\"1\",\"limit\":\"10\"}".data(using: .utf8))
        let expectedQueryParametersMap = try CodableHelper.jsonDecoder.decode(
            [String: String?].self,
            from: expectedQueryParameters
        )

        XCTAssertEqual(echoResponse.queryParameters, expectedQueryParametersMap)
    }

    /// publishAgent
    func testPublishAgentTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.publishAgentWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponseBodyData, "{}".data(using: .utf8))

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/publish")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// unpublishAgent
    func testUnpublishAgentTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.unpublishAgentWithHTTPInfo(agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15")
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)

        XCTAssertEqual(echoResponseBodyData, "{}".data(using: .utf8))

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15/unpublish")
        XCTAssertEqual(echoResponse.method, HTTPMethod.post)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// updateAgent with minimal parameters
    func testUpdateAgentTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.updateAgentWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            agentConfigUpdate: AgentConfigUpdate(name: "updated-agent-name")
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"updated-agent-name\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15")
        XCTAssertEqual(echoResponse.method, HTTPMethod.patch)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// updateAgent with all parameters
    func testUpdateAgentTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.updateAgentWithHTTPInfo(
            agentId: "76710f1b-8231-42e5-b0d1-f43aac618e15",
            agentConfigUpdate: AgentConfigUpdate(
                name: "updated-agent",
                description: "Updated description",
                providerId: "new-provider-id",
                model: "gpt-4o",
                instructions: "Updated instructions.",
                config: ["temperature": 0.5],
                tools: [ToolConfigInput.algoliaDisplayResultsToolConfig(AlgoliaDisplayResultsToolConfig(type: "start"))]
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"updated-agent\",\"description\":\"Updated description\",\"providerId\":\"new-provider-id\",\"model\":\"gpt-4o\",\"instructions\":\"Updated instructions.\",\"config\":{\"temperature\":0.5},\"tools\":[{\"type\":\"start\"}]}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/agents/76710f1b-8231-42e5-b0d1-f43aac618e15")
        XCTAssertEqual(echoResponse.method, HTTPMethod.patch)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// updateConfiguration
    func testUpdateConfigurationTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client
            .updateConfigurationWithHTTPInfo(applicationConfigPatch: ApplicationConfigPatch(maxRetentionDays: 30))
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"maxRetentionDays\":30}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/configuration")
        XCTAssertEqual(echoResponse.method, HTTPMethod.patch)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// updateProvider
    func testUpdateProviderTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.updateProviderWithHTTPInfo(
            providerId: "c2905529-b933-4b69-87ec-75f9829d5f59",
            providerAuthenticationPatch: ProviderAuthenticationPatch(name: "Updated Provider Name")
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"Updated Provider Name\"}".data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59")
        XCTAssertEqual(echoResponse.method, HTTPMethod.patch)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// updateProvider with input
    func testUpdateProviderTest1() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.updateProviderWithHTTPInfo(
            providerId: "c2905529-b933-4b69-87ec-75f9829d5f59",
            providerAuthenticationPatch: ProviderAuthenticationPatch(
                name: "Updated Provider",
                input: ProviderInputNullable.openAIProviderInput(OpenAIProviderInput(apiKey: "sk-new-key-5678"))
            )
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"Updated Provider\",\"input\":{\"apiKey\":\"sk-new-key-5678\"}}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/providers/c2905529-b933-4b69-87ec-75f9829d5f59")
        XCTAssertEqual(echoResponse.method, HTTPMethod.patch)

        XCTAssertNil(echoResponse.queryParameters)
    }

    /// updateSecretKey
    func testUpdateSecretKeyTest() async throws {
        let configuration = try AgentStudioClientConfiguration(
            appID: AgentStudioClientRequestsTests.APPLICATION_ID,
            apiKey: AgentStudioClientRequestsTests.API_KEY
        )
        let transporter = Transporter(configuration: configuration, requestBuilder: EchoRequestBuilder())
        let client = AgentStudioClient(configuration: configuration, transporter: transporter)

        let response = try await client.updateSecretKeyWithHTTPInfo(
            secretKeyId: "a1b2c3d4-5678-90ab-cdef-123456789abc",
            secretKeyPatch: SecretKeyPatch(name: "renamed-key", agentIds: ["76710f1b-8231-42e5-b0d1-f43aac618e15"])
        )
        let responseBodyData = try XCTUnwrap(response.bodyData)
        let echoResponse = try CodableHelper.jsonDecoder.decode(EchoResponse.self, from: responseBodyData)

        let echoResponseBodyData = try XCTUnwrap(echoResponse.originalBodyData)
        let echoResponseBodyJSON = try XCTUnwrap(echoResponseBodyData.jsonString)

        let expectedBodyData = "{\"name\":\"renamed-key\",\"agentIds\":[\"76710f1b-8231-42e5-b0d1-f43aac618e15\"]}"
            .data(using: .utf8)
        let expectedBodyJSON = try XCTUnwrap(expectedBodyData?.jsonString)

        XCTAssertEqual(echoResponseBodyJSON, expectedBodyJSON)

        XCTAssertEqual(echoResponse.path, "/agent-studio/1/secret-keys/a1b2c3d4-5678-90ab-cdef-123456789abc")
        XCTAssertEqual(echoResponse.method, HTTPMethod.patch)

        XCTAssertNil(echoResponse.queryParameters)
    }
}

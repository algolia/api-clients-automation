// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
import { describe, expect, test } from 'vitest';

import { agentStudioClient } from '@algolia/agent-studio';
import type { EchoResponse } from '@algolia/requester-testing';
import { nodeEchoRequester } from '@algolia/requester-testing';

const appId = process.env.ALGOLIA_APPLICATION_ID || 'test_app_id';
const apiKey = process.env.ALGOLIA_SEARCH_KEY || 'test_api_key';

// this makes sure the types are correctly exported
const client = agentStudioClient(appId, apiKey, 'us', { requester: nodeEchoRequester() });

describe('createAgent', () => {
  test('createAgent with minimal parameters', async () => {
    const req = (await client.createAgent({
      name: 'test-agent',
      instructions: 'You are a helpful assistant.',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ name: 'test-agent', instructions: 'You are a helpful assistant.' });
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('createAgent with all parameters', async () => {
    const req = (await client.createAgent({
      name: 'test-agent',
      description: 'A test agent for CTS',
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
      model: 'gpt-4',
      instructions: 'You are a helpful assistant.',
      config: { sendUsage: true, sendReasoning: true, temperature: 0.7, max_tokens: 1500 },
      tools: [{ type: 'start' }],
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({
      name: 'test-agent',
      description: 'A test agent for CTS',
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
      model: 'gpt-4',
      instructions: 'You are a helpful assistant.',
      config: { sendUsage: true, sendReasoning: true, temperature: 0.7, max_tokens: 1500 },
      tools: [{ type: 'start' }],
    });
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('createAgentCompletion', () => {
  test('createAgentCompletion with v4 messages', async () => {
    const req = (await client.createAgentCompletion({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      compatibilityMode: 'ai-sdk-4',
      agentCompletionRequest: { messages: [{ role: 'user', content: 'Hello, how are you?' }] },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/completions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ messages: [{ role: 'user', content: 'Hello, how are you?' }] });
    expect(req.searchParams).toStrictEqual({ compatibilityMode: 'ai-sdk-4' });
  });

  test('createAgentCompletion with v5 messages and all query params', async () => {
    const req = (await client.createAgentCompletion({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      compatibilityMode: 'ai-sdk-5',
      agentCompletionRequest: {
        messages: [{ role: 'user', parts: [{ type: 'text', text: 'What is Algolia?' }] }],
        id: 'test-conversation-id',
      },
      stream: false,
      cache: false,
      memory: false,
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/completions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({
      messages: [{ role: 'user', parts: [{ type: 'text', text: 'What is Algolia?' }] }],
      id: 'test-conversation-id',
    });
    expect(req.searchParams).toStrictEqual({
      compatibilityMode: 'ai-sdk-5',
      stream: 'false',
      cache: 'false',
      memory: 'false',
    });
  });

  test('createAgentCompletion with test configuration', async () => {
    const req = (await client.createAgentCompletion({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      compatibilityMode: 'ai-sdk-4',
      agentCompletionRequest: {
        messages: [{ role: 'user', content: 'Hello' }],
        configuration: {
          instructions: 'Test instructions override',
          config: { temperature: 0.2 },
          tools: [{ type: 'start' }],
        },
      },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/completions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({
      messages: [{ role: 'user', content: 'Hello' }],
      configuration: {
        instructions: 'Test instructions override',
        config: { temperature: 0.2 },
        tools: [{ type: 'start' }],
      },
    });
    expect(req.searchParams).toStrictEqual({ compatibilityMode: 'ai-sdk-4' });
  });
});

describe('createProvider', () => {
  test('createProvider with OpenAI', async () => {
    const req = (await client.createProvider({
      name: 'My OpenAI Provider',
      providerName: 'openai',
      input: { apiKey: 'sk-test-key-1234' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({
      name: 'My OpenAI Provider',
      providerName: 'openai',
      input: { apiKey: 'sk-test-key-1234' },
    });
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('createProvider with Azure OpenAI', async () => {
    const req = (await client.createProvider({
      name: 'My Azure Provider',
      providerName: 'azure_openai',
      input: {
        apiKey: 'az-test-key-5678',
        azureEndpoint: 'https://my-resource.openai.azure.com',
        azureDeployment: 'gpt-4o',
      },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({
      name: 'My Azure Provider',
      providerName: 'azure_openai',
      input: {
        apiKey: 'az-test-key-5678',
        azureEndpoint: 'https://my-resource.openai.azure.com',
        azureDeployment: 'gpt-4o',
      },
    });
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('customDelete', () => {
  test('allow del method for a custom path with minimal parameters', async () => {
    const req = (await client.customDelete({ path: 'test/minimal' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/minimal');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('allow del method for a custom path with all parameters', async () => {
    const req = (await client.customDelete({
      path: 'test/all',
      parameters: { query: 'parameters' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/all');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ query: 'parameters' });
  });
});

describe('customGet', () => {
  test('allow get method for a custom path with minimal parameters', async () => {
    const req = (await client.customGet({ path: 'test/minimal' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/minimal');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('allow get method for a custom path with all parameters', async () => {
    const req = (await client.customGet({
      path: 'test/all',
      parameters: { query: 'parameters with space' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/all');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ query: 'parameters%20with%20space' });
  });

  test('requestOptions should be escaped too', async () => {
    const req = (await client.customGet(
      { path: 'test/all', parameters: { query: 'to be overridden' } },
      {
        queryParameters: { query: 'parameters with space', 'and an array': ['array', 'with spaces'] },
        headers: { 'x-header-1': 'spaces are left alone' },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/all');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({
      query: 'parameters%20with%20space',
      'and%20an%20array': 'array%2Cwith%20spaces',
    });
    expect(req.headers).toEqual(expect.objectContaining({ 'x-header-1': 'spaces are left alone' }));
  });
});

describe('customPost', () => {
  test('allow post method for a custom path with minimal parameters', async () => {
    const req = (await client.customPost({ path: 'test/minimal' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/minimal');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({});
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('allow post method for a custom path with all parameters', async () => {
    const req = (await client.customPost({
      path: 'test/all',
      parameters: { query: 'parameters' },
      body: { body: 'parameters' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/all');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ body: 'parameters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters' });
  });

  test('requestOptions can override default query parameters', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { query: 'myQueryParameter' },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'myQueryParameter' });
  });

  test('requestOptions merges query parameters with default ones', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { query2: 'myQueryParameter' },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters', query2: 'myQueryParameter' });
  });

  test('requestOptions can override default headers', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        headers: { 'x-algolia-api-key': 'ALGOLIA_API_KEY' },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters' });
    expect(req.headers).toEqual(expect.objectContaining({ 'x-algolia-api-key': 'ALGOLIA_API_KEY' }));
  });

  test('requestOptions merges headers with default ones', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        headers: { 'x-algolia-api-key': 'ALGOLIA_API_KEY' },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters' });
    expect(req.headers).toEqual(expect.objectContaining({ 'x-algolia-api-key': 'ALGOLIA_API_KEY' }));
  });

  test('requestOptions queryParameters accepts booleans', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { isItWorking: true },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters', isItWorking: 'true' });
  });

  test('requestOptions queryParameters accepts integers', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { myParam: 2 },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters', myParam: '2' });
  });

  test('requestOptions queryParameters accepts list of string', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { myParam: ['b and c', 'd'] },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters', myParam: 'b%20and%20c%2Cd' });
  });

  test('requestOptions queryParameters accepts list of booleans', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { myParam: [true, true, false] },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters', myParam: 'true%2Ctrue%2Cfalse' });
  });

  test('requestOptions queryParameters accepts list of integers', async () => {
    const req = (await client.customPost(
      { path: 'test/requestOptions', parameters: { query: 'parameters' }, body: { facet: 'filters' } },
      {
        queryParameters: { myParam: [1, 2] },
      },
    )) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/requestOptions');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual({ facet: 'filters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters', myParam: '1%2C2' });
  });
});

describe('customPut', () => {
  test('allow put method for a custom path with minimal parameters', async () => {
    const req = (await client.customPut({ path: 'test/minimal' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/minimal');
    expect(req.method).toEqual('PUT');
    expect(req.data).toEqual({});
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('allow put method for a custom path with all parameters', async () => {
    const req = (await client.customPut({
      path: 'test/all',
      parameters: { query: 'parameters' },
      body: { body: 'parameters' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/test/all');
    expect(req.method).toEqual('PUT');
    expect(req.data).toEqual({ body: 'parameters' });
    expect(req.searchParams).toStrictEqual({ query: 'parameters' });
  });
});

describe('deleteAgent', () => {
  test('deleteAgent', async () => {
    const req = (await client.deleteAgent({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('deleteAgentConversations', () => {
  test('deleteAgentConversations without filters', async () => {
    const req = (await client.deleteAgentConversations({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('deleteAgentConversations with date filters', async () => {
    const req = (await client.deleteAgentConversations({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      startDate: '2024-01-01',
      endDate: '2024-06-30',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ startDate: '2024-01-01', endDate: '2024-06-30' });
  });
});

describe('deleteConversation', () => {
  test('deleteConversation', async () => {
    const req = (await client.deleteConversation({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      conversationId: 'test-conversation-id',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations/test-conversation-id');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('deleteProvider', () => {
  test('deleteProvider', async () => {
    const req = (await client.deleteProvider({
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/5e33d805-7892-4329-bda7-b524dc3fd04a');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('deleteUserData', () => {
  test('deleteUserData', async () => {
    const req = (await client.deleteUserData({ userToken: 'test-user-token' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/user-data/test-user-token');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('exportConversations', () => {
  test('exportConversations without filters', async () => {
    const req = (await client.exportConversations({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations/export');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('exportConversations with date filters', async () => {
    const req = (await client.exportConversations({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      startDate: '2024-01-01',
      endDate: '2024-12-31',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations/export');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ startDate: '2024-01-01', endDate: '2024-12-31' });
  });
});

describe('getAgent', () => {
  test('getAgent', async () => {
    const req = (await client.getAgent({ agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('e2e get agent', async () => {
    const req = (await client.getAgent({ agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('getApplicationConfiguration', () => {
  test('getApplicationConfiguration', async () => {
    const req = (await client.getApplicationConfiguration()) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/configuration');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('e2e getApplicationConfiguration', async () => {
    const req = (await client.getApplicationConfiguration()) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/configuration');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('getConversation', () => {
  test('getConversation', async () => {
    const req = (await client.getConversation({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      conversationId: 'test-conversation-id',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations/test-conversation-id');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('getProvider', () => {
  test('getProvider', async () => {
    const req = (await client.getProvider({
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/5e33d805-7892-4329-bda7-b524dc3fd04a');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('e2e get provider', async () => {
    const req = (await client.getProvider({
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/5e33d805-7892-4329-bda7-b524dc3fd04a');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('getUserData', () => {
  test('getUserData', async () => {
    const req = (await client.getUserData({ userToken: 'test-user-token' })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/user-data/test-user-token');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('invalidateAgentCache', () => {
  test('invalidateAgentCache without before', async () => {
    const req = (await client.invalidateAgentCache({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/cache');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('invalidateAgentCache with before date', async () => {
    const req = (await client.invalidateAgentCache({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      before: '2024-12-01',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/cache');
    expect(req.method).toEqual('DELETE');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ before: '2024-12-01' });
  });
});

describe('listAgentConversations', () => {
  test('listAgentConversations with minimal parameters', async () => {
    const req = (await client.listAgentConversations({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('listAgentConversations with all parameters', async () => {
    const req = (await client.listAgentConversations({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      startDate: '2024-01-01',
      endDate: '2024-12-31',
      page: 2,
      limit: 10,
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/conversations');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ startDate: '2024-01-01', endDate: '2024-12-31', page: '2', limit: '10' });
  });
});

describe('listAgents', () => {
  test('list agents with no params', async () => {
    const req = (await client.listAgents()) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('list agents with all parameters', async () => {
    const req = (await client.listAgents({
      page: 2,
      limit: 5,
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({
      page: '2',
      limit: '5',
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
    });
  });

  test('e2e list agents', async () => {
    const req = (await client.listAgents({ page: 1, limit: 2 })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ page: '1', limit: '2' });
  });
});

describe('listModels', () => {
  test('listModels', async () => {
    const req = (await client.listModels()) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/models');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('e2e list models', async () => {
    const req = (await client.listModels()) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/models');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('listProviderModels', () => {
  test('listProviderModels', async () => {
    const req = (await client.listProviderModels({
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/5e33d805-7892-4329-bda7-b524dc3fd04a/models');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('listProviders', () => {
  test('listProviders with no params', async () => {
    const req = (await client.listProviders()) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('listProviders with pagination', async () => {
    const req = (await client.listProviders({ page: 2, limit: 5 })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ page: '2', limit: '5' });
  });

  test('e2e list providers', async () => {
    const req = (await client.listProviders({ page: 1, limit: 2 })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers');
    expect(req.method).toEqual('GET');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual({ page: '1', limit: '2' });
  });
});

describe('publishAgent', () => {
  test('publishAgent', async () => {
    const req = (await client.publishAgent({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/publish');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('unpublishAgent', () => {
  test('unpublishAgent', async () => {
    const req = (await client.unpublishAgent({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff/unpublish');
    expect(req.method).toEqual('POST');
    expect(req.data).toEqual(undefined);
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('updateAgent', () => {
  test('updateAgent with minimal parameters', async () => {
    const req = (await client.updateAgent({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      agentConfigUpdate: { name: 'updated-agent-name' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff');
    expect(req.method).toEqual('PATCH');
    expect(req.data).toEqual({ name: 'updated-agent-name' });
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('updateAgent with all parameters', async () => {
    const req = (await client.updateAgent({
      agentId: '785b14e4-61a8-4c03-8028-bf2ba3ae85ff',
      agentConfigUpdate: {
        name: 'updated-agent',
        description: 'Updated description',
        providerId: 'new-provider-id',
        model: 'gpt-4o',
        instructions: 'Updated instructions.',
        config: { temperature: 0.5 },
        tools: [{ type: 'start' }],
      },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/agents/785b14e4-61a8-4c03-8028-bf2ba3ae85ff');
    expect(req.method).toEqual('PATCH');
    expect(req.data).toEqual({
      name: 'updated-agent',
      description: 'Updated description',
      providerId: 'new-provider-id',
      model: 'gpt-4o',
      instructions: 'Updated instructions.',
      config: { temperature: 0.5 },
      tools: [{ type: 'start' }],
    });
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('updateConfiguration', () => {
  test('updateConfiguration', async () => {
    const req = (await client.updateConfiguration({ maxRetentionDays: 30 })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/configuration');
    expect(req.method).toEqual('PATCH');
    expect(req.data).toEqual({ maxRetentionDays: 30 });
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

describe('updateProvider', () => {
  test('updateProvider', async () => {
    const req = (await client.updateProvider({
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
      providerAuthenticationPatch: { name: 'Updated Provider Name' },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/5e33d805-7892-4329-bda7-b524dc3fd04a');
    expect(req.method).toEqual('PATCH');
    expect(req.data).toEqual({ name: 'Updated Provider Name' });
    expect(req.searchParams).toStrictEqual(undefined);
  });

  test('updateProvider with input', async () => {
    const req = (await client.updateProvider({
      providerId: '5e33d805-7892-4329-bda7-b524dc3fd04a',
      providerAuthenticationPatch: { name: 'Updated Provider', input: { apiKey: 'sk-new-key-5678' } },
    })) as unknown as EchoResponse;

    expect(req.path).toEqual('/1/providers/5e33d805-7892-4329-bda7-b524dc3fd04a');
    expect(req.method).toEqual('PATCH');
    expect(req.data).toEqual({ name: 'Updated Provider', input: { apiKey: 'sk-new-key-5678' } });
    expect(req.searchParams).toStrictEqual(undefined);
  });
});

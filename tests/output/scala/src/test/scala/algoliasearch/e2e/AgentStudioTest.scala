// Code generated by OpenAPI Generator (https://openapi-generator.tech), manual changes will be lost - read more on https://github.com/algolia/api-clients-automation. DO NOT EDIT.
package algoliasearch.e2e

import algoliasearch.api.AgentStudioClient
import algoliasearch.config.*
import algoliasearch.agentstudio.*
import org.json4s.*
import org.json4s.native.JsonParser.*
import org.scalatest.funsuite.AnyFunSuite
import io.github.cdimascio.dotenv.Dotenv
import org.json4s.native.Serialization
import org.json4s.native.Serialization.write

import java.util.concurrent.TimeUnit
import scala.concurrent.duration.Duration
import scala.concurrent.{Await, ExecutionContextExecutor}

class AgentStudioTest extends AnyFunSuite {
  implicit val ec: ExecutionContextExecutor = scala.concurrent.ExecutionContext.global
  implicit val formats: Formats = JsonSupport.format

  def testClient(): AgentStudioClient = {
    if (System.getenv("CI") == "true") {
      AgentStudioClient(
        appId = System.getenv("ALGOLIA_APPLICATION_ID"),
        apiKey = System.getenv("ALGOLIA_ADMIN_KEY")
      )
    } else {
      val dotenv = Dotenv.configure.directory("../../").load
      AgentStudioClient(
        appId = dotenv.get("ALGOLIA_APPLICATION_ID"),
        apiKey = dotenv.get("ALGOLIA_ADMIN_KEY")
      )
    }
  }

  test("e2e export conversations2") {
    val client = testClient()
    val future = client.exportConversations(
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15"
    )

    val response = Await.result(future, Duration.Inf)
  }

  test("e2e get agent1") {
    val client = testClient()
    val future = client.getAgent(
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15"
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse(
      """{"id":"76710f1b-8231-42e5-b0d1-f43aac618e15","name":"cts_e2e_agent","status":"published","instructions":"You are a helpful assistant for CTS e2e testing."}"""
    )
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e get allowed domain1") {
    val client = testClient()
    val future = client.getAllowedDomain(
      domainId = "6e8a0441-9a41-477c-a1d6-679a461e0990",
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15"
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse(
      """{"id":"6e8a0441-9a41-477c-a1d6-679a461e0990","domain":"cts-e2e.algolia.com","agentId":"76710f1b-8231-42e5-b0d1-f43aac618e15"}"""
    )
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e getConfiguration1") {
    val client = testClient()
    val future = client.getConfiguration(
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse("""{"maxRetentionDays":90}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e get conversation1") {
    val client = testClient()
    val future = client.getConversation(
      conversationId = "alg_cnv_miss_yqcZtaOSPTF8bJsJ",
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15"
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse(
      """{"id":"alg_cnv_miss_yqcZtaOSPTF8bJsJ","agentId":"76710f1b-8231-42e5-b0d1-f43aac618e15","title":"General Greeting"}"""
    )
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e get provider1") {
    val client = testClient()
    val future = client.getProvider(
      providerId = "c2905529-b933-4b69-87ec-75f9829d5f59"
    )

    val response = Await.result(future, Duration.Inf)
    val expected =
      parse("""{"id":"c2905529-b933-4b69-87ec-75f9829d5f59","name":"cts_e2e_provider","providerName":"openai"}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e get secret key1") {
    val client = testClient()
    val future = client.getSecretKey(
      secretKeyId = "c110e615-7d5a-4103-865f-c5062c0d6f4d"
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse("""{"id":"c110e615-7d5a-4103-865f-c5062c0d6f4d","name":"cts_e2e_secret_key"}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e list agent allowed domains1") {
    val client = testClient()
    val future = client.listAgentAllowedDomains(
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15"
    )

    val response = Await.result(future, Duration.Inf)
    val expected =
      parse("""{"domains":[{"id":"6e8a0441-9a41-477c-a1d6-679a461e0990","domain":"cts-e2e.algolia.com"}]}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("listAgentConversations with all parameters1") {
    val client = testClient()
    val future = client.listAgentConversations(
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15",
      startDate = Some("2024-01-01"),
      endDate = Some("2024-12-31"),
      includeFeedback = Some(true),
      feedbackVote = Some(1),
      page = Some(2),
      limit = Some(10),
      xAlgoliaSecureUserToken = None
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse("""{"data":[],"pagination":{"page":2,"limit":10,"totalCount":0,"totalPages":0}}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e list agent conversations2") {
    val client = testClient()
    val future = client.listAgentConversations(
      agentId = "76710f1b-8231-42e5-b0d1-f43aac618e15"
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse(
      """{"data":[{"id":"alg_cnv_miss_yqcZtaOSPTF8bJsJ","agentId":"76710f1b-8231-42e5-b0d1-f43aac618e15","title":"General Greeting"}]}"""
    )
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e list agents2") {
    val client = testClient()
    val future = client.listAgents(
      page = Some(1),
      limit = Some(2),
      providerId = None
    )

    val response = Await.result(future, Duration.Inf)
    val expected =
      parse("""{"data":[{"name":"cts_e2e_agent","status":"published"}],"pagination":{"page":1,"limit":2}}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e list models1") {
    val client = testClient()
    val future = client.listModels(
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse("""{"openai":["gpt-4"]}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e list provider models1") {
    val client = testClient()
    val future = client.listProviderModels(
      providerId = "c2905529-b933-4b69-87ec-75f9829d5f59"
    )

    val response = Await.result(future, Duration.Inf)
  }

  test("e2e list providers2") {
    val client = testClient()
    val future = client.listProviders(
      page = Some(1),
      limit = Some(2)
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse("""{"data":[{"name":"cts_e2e_provider"}],"pagination":{"page":1,"limit":2}}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

  test("e2e list secret keys2") {
    val client = testClient()
    val future = client.listSecretKeys(
      page = Some(1),
      limit = Some(10)
    )

    val response = Await.result(future, Duration.Inf)
    val expected = parse("""{"data":[{"name":"cts_e2e_secret_key"}],"pagination":{"page":1,"limit":10}}""")
    val extracted = Extraction.decompose(response)
    val diffRes = expected.diff(extracted)
    if (diffRes.deleted != JNothing) {
      println(s"This was expected and not found in the deserialized response: ${write(diffRes.deleted)}")
    }
    if (diffRes.changed != JNothing) {
      println(
        s"The expectation was different than what was found in the deserialized response: ${write(diffRes.changed)}"
      )
    }
    if (diffRes.deleted != JNothing || diffRes.changed != JNothing) {
      fail("there is a difference between received and expected")
    }
  }

}
